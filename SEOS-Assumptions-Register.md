# SEOS — Assumptions Register (v0)

Chaque hypothèse est numérotée, versionnée, et reliée à ce qui en dépend. Si une hypothèse est réfutée, tout ce qui est listé dans sa colonne "dépend d'elle" doit être révisé — c'est le seul but de ce document. Aucune hypothèse ici n'est une vérité acquise, y compris celles qualifiées de "choix de conception" (elles restent réversibles, juste non falsifiables par nature).

| ID | Hypothèse | Type | Statut | Dépend d'elle |
|---|---|---|---|---|
| A001 | Une responsabilité architecturale peut être décrite indépendamment d'un langage donné. | Empirique | Non testé | AOC, Rule 0, toute la Research Question |
| A002 | Un Pattern est identifiable par une procédure purement structurelle (fréquence, co-occurrence, isomorphisme de graphe d'imports) sans jugement sémantique. | Empirique | **Toujours vrai comme fait partiel** (Expérience 001) — mais révisé à la baisse comme principe : la procédure structurelle capture le Pattern, pas l'Intent métier (voir Expérience 004 : le "pourquoi" 4 champs obligatoires n'est visible qu'en lisant le corps d'une fonction, pas dans le graphe). N'implique plus "sans jugement sémantique par principe", seulement "sans jugement sémantique dans cette procédure-là". | Charter §4 (révisé, voir §0bis) |
| A003 | Une Intent ne peut jamais être déduite automatiquement du code, seulement déclarée par un humain. | Choix de conception | **Abandonné (révisé, Charter §0bis)** — remplacé par la catégorie "connaissance inférée" : une IA peut produire une hypothèse d'Intent depuis le code (Expérience 004), à condition qu'elle reste vérifiée élément par élément avant promotion en connaissance déclarée. Ce n'est plus "jamais", c'est "jamais sans vérification". | Charter §4 (révisé) |
| A004 | Les "lignées" définies par ascendance technique/culturelle (Charter §2) forment une partition pertinente pour tester la convergence. | Empirique | Non testé | Rule 0, Convergence Review — si Rust et Go s'avèrent aussi éloignés l'un de l'autre que de Solana, la partition en 5 lignées doit être revue |
| A005 | Une projection (Intent → Pattern par lignée) peut préserver les invariants qu'elle prétend transporter. | Empirique | Non testé, aucun exemple vérifié à ce jour | Définition de "Projection", Rule 0 critère 4 |
| A006 | Un corpus de 3 à 5 responsabilités par lignée, avec un seuil de 20 %/50 % d'échappatoires, suffit pour trancher H0 vs H1. | Choix de conception arbitraire | Non validé — à réviser si le premier test rend systématiquement un résultat "non concluant" | Charter §1, protocole de falsification |
| A007 | Un humain peut annoter le lien Pattern → Intent de façon reproductible, sans biais de confirmation (voir l'Intent qu'il s'attend à trouver plutôt que celle réellement présente). | Empirique | Non testé — risque méthodologique réel, pas seulement théorique | Fiabilité de toute la connaissance déclarée (Charter §4) |
| A009 | Le pattern "assertion function de validation de domaine" (voir Expérience 005) est un candidat légitime au noyau canonique. | Empirique | **Revu à la baisse (addendum Expérience 005)** — vérification du contenu réel montre 8 fichiers quasi identiques (copier-coller), pas 3 inventions independantes ; critère 1 de Rule 0 probablement non rempli. Promotion refusée, motif renforcé. | `crud-entity.pattern.json` → `extension_candidates.domain_validators_assertion_pattern` |

## Expérience 001 — Pattern extraction sans code custom (madge)

Date : suite à la directive de co-recherche. Protocole : appliquer `madge` (outil open-source existant, résolution de graphe de dépendances TypeScript, aucune IA) sur `src/presentation/pages/administrative-boundary`, avec le `tsconfig.app.json` du projet pour résoudre les alias de chemin (`@pages/*`, `@shared/*`). Puis normalisation par script (25 lignes) remplaçant les noms d'entité (`departments`/`municipalities`/`regions`) par un jeton générique dans les chemins de dépendances.

**Résultat mesuré** : sur 5 types de facades identifiés, 3 (`ENTITY.facade.ts`, `ENTITY-select.facade.ts`, `ENTITY-find-one.facade.ts`) ont une signature de dépendances strictement identique entre `regions`, `departments` et `municipalities` après normalisation — sans qu'aucune ligne de code n'ait été écrite pour "connaître" le pattern CRUD à l'avance. Les 2 autres (`municipalities-by-department-id.facade.ts`, `ENTITY-by-region-id.facade.ts`) n'apparaissent qu'une seule fois chacun (n=1) : pas un échec de la méthode, juste un échantillon insuffisant pour tester une répétition sur ces facades relationnelles spécifiques.

**Conclusion (statut : fait observé, pas une théorie)** : la couche "Pattern" du projet (Charter §7, détection structurelle) n'est pas un problème de recherche non résolu — c'est un problème déjà largement couvert par des outils existants (madge, dependency-cruiser, et plus largement le champ de l'*Architecture Recovery* : Rigi, Bauhaus, Lattix, Structure101, actif depuis les années 1990) combinés à un script de normalisation trivial. Ceci révise à la baisse le coût estimé de la Phase "Pattern Extraction Engine" de la roadmap — il ne s'agit probablement pas de construire un extracteur, mais d'assembler un outil existant + une normalisation fine.

**Limite explicite** : la normalisation utilisée ici connaît à l'avance les 3 noms d'entité — c'est un raccourci manuel, pas une découverte automatique de "quel token correspond à une entité variable". Généraliser cette normalisation (détecter automatiquement quelle partie d'un chemin varie d'une instance à l'autre) reste un problème ouvert, mais nettement plus petit que "construire un analyseur d'architecture depuis zéro".

## Expérience 002 — Recouvrement structurel sans aucun nom (anonymisation complète)

Protocole : sur le même corpus (`administrative-boundary`), construire un graphe où les identifiants ne sont utilisés que pour sélectionner l'ensemble de fichiers "cœur CRUD" par entité (en excluant les fichiers de relation croisée type `-by-` ou qui référencent une deuxième entité) puis anonymiser tous les nœuds. Un algorithme de raffinement de couleurs (1-WL, standard en isomorphisme de graphes) calcule ensuite une signature purement structurelle (degrés, cardinalités, voisinage) sans jamais lire un nom de fichier. Contrôle négatif inclus : une topologie randomisée à degrés équivalents est testée pour vérifier que la méthode sait dire "non" quand la structure diffère réellement.

**Résultat mesuré, à deux granularités différentes :**

- **Grain fin (profil de degré immédiat, 1 saut)** : les profils de degrés des ~90 fichiers "cœur CRUD" de `departments`, `regions` et `municipalities`, triés, sont quasi identiques terme à terme.
- **Grain large (raffinement complet sur plusieurs sauts, graphe combiné des 3 entités)** : les classes structurelles finales des facades divergent (`departments`, `regions`, `municipalities` n'atterrissent pas dans la même classe après 4 itérations de raffinement).
- **Contrôle négatif** : une topologie randomisée à mêmes degrés ne produit pas la même signature qu'une topologie réelle — la méthode distingue donc bien le signal du bruit, elle ne dit pas "identique" par défaut.

**Interprétation (hypothèse, pas un fait établi)** : la divergence au grain large coïncide avec une différence réelle et documentable du domaine métier — `regions` est la racine d'une hiérarchie à 3 niveaux (région → département → municipalité), `departments` est au milieu (une référence parent), `municipalities` est la feuille (une référence parent). Le raffinement profond capte donc peut-être une vraie asymétrie sémantique du domaine plutôt qu'un bruit de mesure. Ce n'est pas vérifié : la cause pourrait aussi être une contamination résiduelle du filtrage (fichiers de relation non totalement exclus).

**Conclusion honnête** : ni "cas 1" (le pattern survit intégralement) ni "cas 2" (le pattern disparaît) tels qu'anticipés. Résultat intermédiaire, plus intéressant que les deux issues prévues : la récupération structurelle sans nom semble tenir à faible profondeur (voisinage immédiat) mais se dégrader à mesure que la comparaison s'étend à plusieurs sauts dans le graphe — possiblement parce qu'à cette profondeur, la structure encode une vraie différence sémantique (la profondeur hiérarchique de chaque entité), pas un artefact.

**Question ouverte** ajoutée au registre : à quelle profondeur de graphe la structure cesse-t-elle d'être indépendante du vocabulaire métier, et cette limite est-elle elle-même un invariant transposable à d'autres lignées ?

## Expérience 003 — Correction méthodologique : la divergence d'Expérience 002 est probablement un artefact de l'outil, pas un phénomène

Suite à la proposition de généraliser Expérience 002 en "Pattern = f(profondeur)" (une fonction continue de similarité qui décroîtrait avec l'échelle), vérification préalable requise avant toute reproduction sur un nouveau pattern : la métrique utilisée dans Expérience 002 (raffinement de couleurs 1-WL) est un test d'isomorphisme **exact et monotone** — par construction, une seule différence exacte n'importe où dans le graphe (par exemple un compteur de dépendance externe qui diffère de 1) crée une classe distincte qui ne peut plus jamais refusionner aux itérations suivantes. Ce n'est pas un détail : 1-WL est conçu pour tester l'égalité stricte, pas la similarité graduée. Or l'hypothèse proposée ("la similarité décroît avec la profondeur") est une hypothèse continue, qui ne peut pas être testée correctement avec un instrument binaire (même classe / classe différente).

**Protocole corrigé** : remplacement du test exact par une métrique de recouvrement graduée (BFS depuis chaque facade, cumul des triplets de degré (out interne, in interne, ext) à chaque profondeur, score de recouvrement = 2×intersection / somme des tailles) sur les 3 mêmes facades qu'Expérience 001/002.

**Résultat mesuré** : avec cette métrique graduée, la similarité reste à 1.000 (recouvrement parfait) de la profondeur 1 à la profondeur 5 pour 2 des 3 types de facade (`facade principale`, `facade find-one`) — c'est-à-dire jusqu'à épuisement du sous-graphe atteignable, sans aucune décroissance. Le troisième type (`facade select`, sous-graphe beaucoup plus petit) descend à 0.778 en profondeur 2 puis remonte à 0.833 en profondeur 3 — une fluctuation sur un très petit échantillon, pas une tendance monotone.

**Conclusion** : la divergence observée dans Expérience 002 (raffinement exact) n'est très probablement **pas** la preuve d'un phénomène multi-échelle réel de l'architecture logicielle. C'est plus probablement un artefact attendu et bien documenté du choix d'un algorithme de test d'égalité stricte appliqué à des données imparfaitement nettoyées. Une fois la métrique corrigée pour tolérer les écarts, la similarité structurelle est stable et élevée sur toute la profondeur testée — elle ne décroît pas.

**Conséquence pour A008** : l'hypothèse "la similarité architecturale est fonction de l'échelle d'observation" n'est donc pas soutenue par les données actuelles une fois l'instrument de mesure corrigé. Elle n'est pas formalisée comme hypothèse active du projet. Elle reste une question légitime pour une prochaine expérience correctement instrumentée dès le départ (métrique graduée, pas de test exact), mais ne doit pas être adoptée sur la base d'Expérience 002 seule, dont le résultat s'explique probablement par le choix d'outil plutôt que par la réalité observée.

## Expérience 004 — Extraction sémantique par IA, vérifiée élément par élément (protocole inspiré d'ArchAgent, échelle réduite)

Contexte : suite à la revue de littérature (Charter §0bis) et à l'acceptation du principe "l'IA doit être au cœur de l'extraction", premier test concret : produire une lecture d'Intent par IA (moi, sur du code réel) puis vérifier chaque affirmation contre le code, plutôt que l'accepter sur confiance. Échelle volontairement réduite par rapport à ArchAgent (pas d'évaluateurs humains, un seul module) — ce n'est pas une réplication du benchmark, seulement une démonstration du protocole vrai/faux appliqué à notre propre corpus.

**Cible** : le flux de création d'`administrative-infrastructure` (`InfrastructureCreateContract` → `infrastructureCreateVo` → `InfrastructureCreateEntity`).

**Hypothèse d'Intent produite par IA (avant vérification)** : "Ce module implémente un pattern *parse, don't validate* : `InfrastructureCreateContract` (tous champs optionnels) représente une entrée non fiable, `InfrastructureCreateValidateContract` (tous champs obligatoires) représente une entrée garantie valide, et la fonction de validation fait le pont entre les deux via une *assertion function* TypeScript (`asserts contract is X`), pas seulement via une convention de nommage."

**Vérification élément par élément (lecture directe du code, pas de confiance sur le modèle)** :

| Affirmation | Vrai/Faux | Preuve |
|---|---|---|
| Les 4 champs sont optionnels dans `InfrastructureCreateContract` | Vrai | `name?`, `type?`, `position?`, `description?` dans `domain/contracts/infrastructure/infrastructure-create.contract.ts` |
| Les 4 champs sont obligatoires dans `InfrastructureCreateValidateContract` | Vrai | mêmes champs sans `?` dans `infrastructure-create.validate-contract.ts` |
| `infrastructureCreateVo` appelle une validation avant de retourner le type strict | Vrai | `validateInfrastructureCreate(contract)` appelé en première ligne dans `infrastructure-create.vo.ts` |
| La validation est une *assertion function* TypeScript, pas une fonction booléenne | Vrai | signature `export function validateInfrastructureCreate(contract): asserts contract is InfrastructureCreateValidateContract` dans `domain/validators/infrastructure/infrastructure-create.validator.ts` |
| Chaque champ manquant lève une erreur métier distincte, avec clé i18n dédiée | Vrai | 4 blocs `if (!contract.X) throw new GenericRequiredError('ADMINISTRATIVE_INFRASTRUCTURE...X_REQUIRE')` |

Les 5 affirmations sont vraies. Le fichier `domain/validators/infrastructure/infrastructure-create.validator.ts` n'avait pas été lu lors de l'audit initial des 11 modules.

**Correction d'une conclusion antérieure (auto-critique, pas un nouveau fait)** : l'audit précédent (section "Correction du socle") avait qualifié `infrastructure-create.validate-contract.ts`, le factory et le commands-mapper de "sans logique réelle malgré le nom" — c'est vrai pour ces 3 fichiers précis (le premier est un type, les deux autres du pass-through pur), mais j'avais généralisé cette observation à "la couche validation/transformation du module", sans avoir lu `domain/validators/*.validator.ts`, où la logique réelle vit effectivement. C'est exactement le type d'erreur que le protocole de vérification élément par élément est censé empêcher : une affirmation vraie sur un sous-ensemble de fichiers, étendue par confiance à un ensemble plus large sans le vérifier. Le principe de rejet de `commands-mappers/contracts/factories` (règle de falsifiabilité, cf. section suivante) reste valide pour CES fichiers précis — mais pas comme jugement sur "la validation du module", qui existe bien et fonctionne.

**Ce que l'extraction structurelle seule (EXP-001/002/003, madge/1-WL) ne peut pas voir** : le graphe de dépendances montre que `infrastructure-create.vo.ts` importe `infrastructure-create.validator.ts` — un lien structurel banal, identique à des centaines d'autres imports du même fichier. Rien dans le graphe ne distingue "cet import est une validation métier avec effet de bord (throw)" d'un import utilitaire quelconque. Voir cette distinction demande de lire le corps de la fonction et sa signature de type (`asserts ... is ...`) — une opération sémantique, pas structurelle.

**Conclusion** : premier résultat positif et vérifié pour "IA au cœur de l'extraction" — une hypothèse d'Intent produite sans lecture préalable exhaustive s'est révélée entièrement vérifiable contre le code, et a corrigé une généralisation excessive faite plus tôt dans ce même projet. Ceci ne prouve pas que toute hypothèse IA future sera juste — ça démontre seulement que le protocole de vérification (Charter §4, "connaissance inférée") fonctionne : il a attrapé et corrigé une erreur, la sienne, avant qu'elle ne se propage plus loin.

## Expérience 005 — Test Rule 0 réel sur le pattern "assertion function de validation de domaine" (pas une intuition, une mesure)

Suite au constat qu'EXP-004 avait vérifié un seul fichier, question de fond : ce pattern (`domain/validators/{ENTITY}/{ENTITY}-{operation}.validator.ts`, garde de type via `asserts contract is X`) est-il un cas isolé ou une convergence réelle ? Recherche exhaustive par `grep -rl "asserts .* is "` sur les 11 modules audités (pas une supposition).

**Résultat mesuré** : le pattern existe dans 3 modules sur 11 — `administrative-infrastructure` (infrastructure : create/filter/update), `communication` (messaging : create/filter/update ; notifications : filter), `report-states` (approve/close/evaluate/reject : download/filter). `administrative-boundary` — le module qui a servi de source unique à `crud-entity.pattern.json` v1/v2/v3 et donc au module de référence "resources" — n'en a **aucun**.

**Évaluation Rule 0 (Charter), critère par critère, pas de raccourci** :
1. Récurrence indépendante — partiellement remplie : 3 modules, 4 types d'opération (create/filter/update/download), plus large que le seul cas "create" d'EXP-004. Limite honnête non résolue : l'indépendance des 3 occurrences (même auteur/période vs redécouvertes séparées) n'est pas vérifiée — nécessiterait l'historique git, non consulté ici.
2. Indépendance technologique — remplie en principe : la technique existe sous d'autres formes dans d'autres langages typés (`TryFrom` en Rust, `require()` en Kotlin).
3. Justifié par un exemple de corpus réel — rempli (EXP-004, 5/5 affirmations vérifiées vraies).
4. Amélioration mesurable de la capacité de projection — **non testé**. Aucune mesure ne montre qu'ajouter ce pattern au générateur réduit un risque réel (ex. état invalide créé par un appel API direct, hors formulaire, dans un module qui ne l'a pas).

**Conclusion (initiale)** : candidat Rule 0 sérieux (3 critères sur 4 plausibles), mais non promouvable en l'état. Ajouté à `crud-entity.pattern.json` → `extension_candidates.domain_validators_assertion_pattern`, statut CANDIDAT.

**Addendum — critère 1 (récurrence indépendante) revu à la baisse après vérification de contenu réelle** : sur demande explicite de vérifier le contenu (pas seulement compter les occurrences), comparaison ligne à ligne des fichiers `*-filter.validator.ts` trouvés dans `administrative-infrastructure`, `communication` (messaging, notifications) et `report-states` (approve, close, evaluate, reject, download) — **8 fichiers, 15 lignes chacun, structure et logique identiques** (seul le nom du type importé change), tous réimplémentant à la main la même vérification `startDate > endDate` que `DatePeriod.create()` fait déjà ailleurs dans le même projet — en moins bien : ces 8 copies ne vérifient pas que les dates sont parseables (`DatePeriod` le fait). `report-states` utilise même les deux conventions en interne selon l'entité (incohérence à l'intérieur d'un seul module, pas seulement entre modules).

Ceci n'est pas une convergence indépendante à 3 (ni 8) occurrences — c'est une duplication par copier-coller, probablement d'un seul fichier source vers les 7 autres. Le critère 1 de Rule 0 passe donc de "partiellement rempli" à **probablement non rempli**. Plus largement : appliquer l'appareillage Rule 0 (conçu pour tester une convergence indépendante entre lignées technologiques distinctes — Angular vs Rust vs Solana) à une question de duplication de code au sein d'un même projet TypeScript était disproportionné — c'est un problème d'ingénierie ordinaire (consolider un doublon, garder `DatePeriod` qui fait strictement plus), pas une question de recherche. Traiter toute répétition de code comme un candidat Rule 0 aurait été la même erreur que "compter les votes dans un seul projet" pour juger une architecture — ce que ce projet, conçu par une seule personne, ne peut de toute façon pas trancher à lui seul : sa majorité interne n'est pas une preuve de qualité externe, seulement une préférence répétée par le même auteur.

**Conséquence pour le module de référence** : aucun changement nécessaire — "resources" n'implémente déjà aucune des deux versions du check date-range à la création (il suit `DatePeriod` pour le filtre, conforme à sa source, et n'a pas de validateur de création, conforme à sa source également). La correction utile ici concerne le code réel du projet, pas SEOS : les 8 fichiers dupliqués devraient être remplacés par `DatePeriod`, mais ce n'est pas une action SEOS — c'est un refactor ordinaire du codebase hôte, hors périmètre de ce registre.

**Conséquence pour la clôture "Étape 1"** : voir correction ci-dessous.

## Correction de périmètre — "Étape 1 close" portait sur la conformité structurelle, pas sur la validation de domaine

La section "Étape 1 close" (ci-dessous) déclarait le module de référence "resources" comme socle vérifié à 100 %. C'est vrai, mais seulement pour le périmètre testé : conformité au schéma v3 (fichiers présents), compilation (`tsc`), style (`eslint`). Ce périmètre ne couvrait pas la dimension révélée ensuite par EXP-004/005 : aucune vérification de domaine (assertion function) n'existe dans "resources", exactement comme dans sa source `administrative-boundary`. Ce n'est pas un défaut caché du module — c'est une fidélité correcte à sa source — mais cela signifie que "close" décrivait un sous-ensemble des critères qui comptent, pas une clôture générale du sujet "validation". Le module de référence n'est pas régénéré pour le pattern d'EXP-005 (candidat non promu, pas une exigence du schéma). Il l'a en revanche été pour un second écart, distinct et plus grave, trouvé juste après : voir Expérience 006 — celui-ci n'était pas un candidat en attente de promotion, c'était une régression par rapport à la source déclarée du module, corrigée directement (v4 du schéma).

## Expérience 006 — Le module de référence "resources" contredisait sa propre source sur la validation de dates (correction appliquée, pas seulement documentée)

Déclenchée par une critique directe sur `resources.handler.ts` (construction d'un littéral inline plutôt qu'un mapper vers un Contract puis un ValidationContract, comme dans `administrative-infrastructure`). Avant d'adopter la proposition telle quelle, vérification complète requise : le littéral inline du handler n'est PAS une anomalie — `departments.handler.ts` (source réelle du schéma) fait exactement la même chose. Le problème n'est pas le handler, il est en amont.

**Recherche systématique** : `grep` de `DatePeriod` (value object partagé, `@shared/domain/value-objects/date-period.vo.ts`, factory `.create()` qui valide 3 invariants réels : date de début parseable, date de fin parseable, début ≤ fin — throw sinon) sur les 11 modules. Résultat : présent dans **8 modules sur 11, 59 fichiers** — très majoritaire, bien plus que le pattern "assertion function" d'Expérience 005 (3/11). `administrative-boundary`, la source déclarée du schéma et du module "resources", l'utilise (`DepartmentsFilterVo.fromDto` construit un `DatePeriod` et trim le `search`). Le module "resources" généré, lui, ne faisait ni l'un ni l'autre : dates brutes non validées, `search` non trimé — une déviation non détectée de sa propre source, invisible pour `check-pattern.js` car cet outil ne vérifie que la présence de fichiers, jamais leur contenu (rappelé explicitement dans la sortie du script).

**Mise en garde épistémologique (ne pas confondre avec Expérience 005)** : `DatePeriod` est une classe partagée réutilisée, pas une invention indépendante retrouvée dans 8 modules séparés — c'est un signal de bonne hygiène d'ingénierie (réutilisation de code), pas une preuve de convergence architecturale indépendante au sens Rule 0. Sa recurrence ne vaut donc pas comme preuve de "pattern universel" — mais elle vaut comme preuve suffisante pour corriger une régression du module de référence par rapport à sa propre source, ce qui ne nécessite aucun critère Rule 0.

**Action appliquée (pas seulement recommandée)** : `seos/tools/generate-reference-module.js` corrigé — `${Cap}FilterProps`/`${Cap}FilterVo`/`${Cap}FilterEntity`/`${E}FilterMapper` utilisent désormais `DatePeriod`, mirroir exact de `DepartmentsFilterVo`/`DepartmentsFilterEntity`/`departmentsFilterMapper`. Module régénéré et reverifié : `tsc --noEmit` 0 erreur, `eslint` 0 erreur (après `prettier --write` + `eslint --fix`), `check-pattern.js` 87/87 (100 %, inchangé — attendu, l'outil ne voit pas ce type de correction).

**Réponse directe à la proposition initiale (mapper → Contract → ValidationContract)** : non retenue telle quelle. Cette proposition reproduit la convention `commands-mappers/contracts` d'`administrative-infrastructure`, minoritaire (2/11 pour le queries-mapper nommé, déjà noté non promu). La correction appliquée suit à la place la convention majoritaire et déjà partagée (`DatePeriod`, 8/11), qui est aussi celle de la source déclarée du module. Le nom donné au littéral (mapper nommé vs inline) reste une question de style non tranchée par les données ; l'absence de validation, elle, était une régression réelle, corrigée.

## Expérience 007 — Décision explicite de l'architecte, adoptée sans attendre de majorité (commands-mappers/queries-mappers)

Directive directe : plus aucun littéral inline dans les handlers (`this.useCase.execute({ search: query.search, ... })`) — remplacé partout par un mapper nommé et importé, sur le modèle d'`infrastructureCreateCommandMapper`. Rappel explicite reçu en même temps : "le code répandu dans les différents modules ne signifie pas qu'il doit être adopté" — et `administrative-boundary` (source historique du schéma) est explicitement qualifié de mal conçu, à ne plus traiter comme référence de qualité par défaut.

**Différence de nature avec Expériences 001-006** : ici, aucune mesure de prévalence n'est invoquée ni requise. C'est une décision de conception assumée par l'architecte du projet, pas une conclusion dérivée d'un audit de corpus — exactement la distinction "connaissance déclarée" du Charter §4. Elle est adoptée telle quelle, sans lui faire passer un test Rule 0 : Rule 0 sert à trancher entre conventions concurrentes quand la preuve est ambiguë, pas à approuver une décision d'architecte déjà prise.

**Action appliquée** : `generate-reference-module.js` modifié — nouveaux fichiers `application/commands-mappers/{ENTITY}/{ENTITY}-{create,update,delete}.mapper.ts` et `application/queries-mappers/{ENTITY}/{ENTITY}.mapper.ts` + `{ENTITY}-find-one.mapper.ts`, tous les handlers (commands ET queries, y compris find-one) appellent désormais leur mapper au lieu de construire un objet inline. `crud-entity.pattern.json` passé en v5, 5 fichiers ajoutés à `core_files` (87 → 92). Module régénéré et reverifié : `tsc` 0 erreur, `eslint` 0 erreur, `check-pattern.js` 92/92.

**Portée de la décision, précisée pour éviter toute confusion future** : seuls `commands-mappers` et `queries-mappers` sont promus. `domain/contracts` et `domain/factories` (le reste du bundle observé dans `administrative-infrastructure`/`communication`) restent explicitement hors du cœur — aucune instruction ni preuve nouvelle ne les concerne, cf. `extension_candidates.commands_mappers_contracts_factories.revision_v5`.

## Note — administrative-boundary ne doit plus être traité comme référence de qualité par défaut

Précision de l'architecte, à conserver pour toute décision future : `administrative-boundary` (le module qui a historiquement fourni la source du schéma v1-v4) est mal conçu. Son usage passé comme "source" a servi à établir des faits d'observation (ce que ce module fait réellement — EXP-001 à 003, la structure des 87 fichiers), pas un jugement de qualité. Toute justification future de type "c'est ce que fait administrative-boundary donc c'est correct" est invalide à partir de maintenant — seule la prévalence réelle sur l'ensemble des 11 modules (avec vérification de contenu, pas de comptage seul, cf. Expérience 006) ou une décision explicite de l'architecte (cf. Expérience 007) valent justification.

## Expérience 008 — Pivot de source (administrative-boundary → administrative-infrastructure/communication) et audit réel des nouvelles références

Directive de l'architecte : `administrative-boundary` est mal conçu et ne doit plus servir de référence. `administrative-infrastructure` et `communication` sont désignés comme bien conçus, avec mandat explicite de les critiquer et de les améliorer (pas de les accepter tels quels), puis de les utiliser comme référence.

**Audit réel effectué avant toute adoption (pas d'acceptation par confiance)** : lecture de tous les commands-handlers/queries-handlers des deux modules.

Résultat, deux problèmes réels trouvés :
1. **Code mort confirmé** (grep de toutes les références, pas seulement local) : `administrative-infrastructure` avait 4 fichiers orphelins — `infrastructure-filter.handler.ts`, `infrastructure-find-one-filter.handler.ts` (entité infrastructure) et leurs équivalents pour infrastructure-type — dupliqués d'un refactor incomplet, jamais injectés nulle part (aucun bus/provider ne les référence), la vraie voie passant par `queries-handlers/*.handler.ts`. Supprimés.
2. **Incohérence interne** : dans `administrative-infrastructure`, l'entité `infrastructure` a des mappers ET des validateurs de domaine réels (create/update/filter) ; l'entité `infrastructure-type`, dans le MÊME module, n'a ni l'un ni l'autre — son handler de création fait `command.name as string` (cast non sûr, aucune validation réelle). `communication` est plus cohérent (pas de code mort trouvé), mais messaging/notifications avaient les mêmes littéraux inline que `resources` pour delete/disable/enable/find-one — seuls create/update avaient des mappers.

**Conclusion** : ces deux modules ne sont pas uniformément excellents — la partie "infrastructure" d'`administrative-infrastructure` (create/update/filter) est le vrai modèle à suivre ; "infrastructure-type" ne l'est pas et reste un point ouvert (nécessite une décision de l'architecte sur les règles métier réelles avant d'y ajouter des validateurs — pas une décision que je prends seul).

**Action appliquée dans le vrai code (pas seulement le module de référence)** : mappers ajoutés à tous les handlers restants de `administrative-infrastructure` et `communication` (delete/disable/enable/find-one, + toute l'entité infrastructure-type pour create/update/delete/disable/enable/filter/find-one). `tsc` (app entière) : 0 erreur. `eslint` : 0 erreur (après nettoyage d'un import mort trouvé au passage dans `infrastructure-type.facade.ts`, non lié à ce chantier mais découvert pendant la vérification).

**Refonte du filtre dans le module de référence** : sur exemple direct de l'architecte (`infrastructureFilterVo`/`validateInfrastructureFilter`), la chaîne de filtre de "resources" est réécrite en style fonctionnel — `resources-filter.contract.ts` (interface), `resources-filter.validator.ts` (assertion function, remplace `DatePeriod`), `resources-filter.vo.ts`/`resources-filter.entity.ts` (fonctions pures, plus de classe). Remplace l'approche EXP-006 (v4, `DatePeriod`-VO-classe). `crud-entity.pattern.json` passé en v6 (93 fichiers). Régénéré et reverifié : `tsc` 0 erreur, `eslint` 0 erreur, `check-pattern.js` 93/93.

**Point de cohérence à noter, pas une contradiction** : ce pattern (assertion function pour le filtre) est exactement celui dont Expérience 005 (addendum) a montré qu'il est dupliqué presque à l'identique dans 8 fichiers du corpus, sans preuve d'indépendance. Ceci n'invalide pas la décision de l'architecte de v6 — EXP-005 répondait à une question différente ("ce pattern mérite-t-il d'être promu par PRÉVALENCE du corpus ?", réponse : non, toujours pas) — pas à celle posée ici ("l'architecte veut-il ce style pour son propre module de référence ?", une décision déclarée, cf. Charter §4, qui ne nécessite pas de preuve de convergence).

**Portée explicitement limitée** : seule la chaîne de validation du filtre a été migrée vers les conventions d'`administrative-infrastructure`. Les autres catégories du schéma (entités create/update, mappers de données, structure de dossiers) suivent encore partiellement `administrative-boundary` et n'ont pas été réévaluées sous cet angle — ce n'est pas une reconstruction complète du socle, seulement le périmètre demandé.

## Expérience 009 — Homogénéisation réelle du pattern mapper sur toute l'application (codemod, pas édition manuelle)

**Pourquoi la migration précédente (EXP-008) n'avait couvert que 2 modules** : discipline de périmètre, pas un oubli — chaque expérience de ce registre a été scopée à ce qui était explicitement demandé, pour éviter de prescrire un changement architectural plus large que ce qui avait été revu par l'architecte. Étendre seul aux 14 autres modules sans direction explicite aurait réintroduit exactement le travers que ce projet corrige depuis le début : décider à la place de l'architecte plutôt qu'avec lui. Sur directive explicite ("applique ces modifications sur tout les modules"), le périmètre est levé.

**Méthode** : un littéral inline répété à l'identique dans ~140 fichiers à travers 14 modules ne se corrige pas de façon fiable à la main — le risque d'erreur (faute de frappe, mauvais nom de champ) sur un volume pareil dépasse largement le risque du problème initial. Écrit un codemod (regex structurel, pas un LLM au milieu du refactor : la transformation est mécanique et le contenu ne varie pas assez pour justifier une génération non déterministe) : détecte `execute(...) { return this.useCase.X({ ...littéral... }, ...) }`, extrait le littéral et le type du paramètre, génère un fichier mapper nommé dans `commands-mappers`/`queries-mappers` (miroir de la convention déjà adoptée), réécrit le handler pour appeler le mapper.

**Résultat mesuré** : 139 fichiers transformés automatiquement (+ 139 fichiers mapper créés) sur 14 modules réels. 37 fichiers correctement ignorés par le script (déjà migrés en EXP-008, ou sans littéral à extraire — ex. `readAll()` sans argument). Code mort supplémentaire trouvé et supprimé au passage : `settings-security/users-filter.handler.ts` et `users-find-one-filter.handler.ts`, même motif exact que dans `administrative-infrastructure` (doublon d'un refactor incomplet, jamais référencé).

**Bug du codemod trouvé et corrigé** : 4 fichiers (`report-states`, mappers de téléchargement approve/close/evaluate/reject) référençaient `DownloadSource` (un enum) à l'intérieur du littéral extrait sans que le script importe ce type dans le nouveau fichier mapper — le script ne copiait que l'import du type du PARAMÈTRE, pas des types utilisés à l'intérieur du littéral lui-même. Corrigé manuellement (4 imports ajoutés aux mappers, 4 imports devenus morts supprimés des handlers). Seule anomalie sur 139 fichiers.

**Vérification** : `tsc -p tsconfig.app.json --noEmit` sur l'application entière : 0 erreur. `eslint` sur les 16 modules (par lots, tous vérifiés individuellement) : 0 erreur après `prettier --write` + `eslint --fix`, plus un import mort non lié trouvé et supprimé dans `reject.repository.impl.ts` (`SimpleResponseDto` inutilisé).

**Ce que "homogène" couvre désormais, et ce qu'il ne couvre pas** : le pattern mapper (handlers sans littéral inline) est maintenant uniforme sur toute l'application — vérifié, pas supposé. La validation de filtre façon `administrative-infrastructure` (Contract + assertion function) ne l'est PAS : seul `resources` (module de référence) l'a adoptée ; les 14 modules réels gardent chacun leur propre convention de validation (`DatePeriod`, rien, ou l'assertion function dupliquée selon les cas — cf. Expérience 005/006). Étendre la validation de filtre à tous les modules réels changerait un comportement métier (quand une erreur est levée), pas seulement une forme de code — ce n'est pas un codemod sûr sans revue entité par entité, contrairement au renommage mapper qui ne change aucun comportement.

**Mesure concrète de l'écart structurel restant (`check-pattern.js` contre le schéma v6, sur du vrai code)** : `administrative-boundary/departments` 88,2 % (82/93), `administrative-infrastructure/infrastructure` 86,0 % (80/93), `communication/messaging` 77,4 % (72/93). Le pattern mapper étant désormais homogène partout, l'écart restant est structurel (arborescence de dossiers : `presentation/features/{ENTITY}` vs `presentation/{ENTITY}`, `presentation/store` vs autres, contrats/validateurs de filtre non généralisés) — un chantier distinct, plus risqué (déplacement de fichiers, pas réécriture de fonctions), non entrepris ici.

## Expérience 010 — Élimination des classes VO/Entity (create/update/delete/filter) au profit de fonctions, sur directive explicite de l'architecte

**Directive reçue** : "je ne veux plus de code tel [...] dans le vo ni dans entity que ce soit à la create filtre, delete, modification etc... inspire-toi de ce qui a été déjà fait dans infrastructure (mets à jour aussi infrastructure)". Contrairement à EXP-007/009 (adoption de mappers), cette directive ne porte pas sur un littéral inline mais sur une couche entière : les classes `{ENTITY}CreateVo`/`{ENTITY}CreateEntity` (et update/delete/filter/disable/enable) avec `static fromDto`/`static fromVo`.

**Constat préalable, vérifié avant toute suppression** : sur l'entité `infrastructure` (déjà migrée avant cette expérience) et sur `infrastructure-type` (migrée ici), les classes Entity de create/update/delete/disable/enable ne contenaient aucune transformation — uniquement un constructeur recopiant les champs et une méthode statique `fromVo` recopiant à nouveau les mêmes champs depuis le VO. Deux méthodes mortes ont été trouvées et confirmées par `grep -rl` scopé au module (aucun appel en dehors de leur propre fichier de définition) : `appliesToAdminScope()` (présente dans les Entity create/delete/disable/enable de `infrastructure-type`) et `describe()` (présente dans les 4 mêmes Entity, y compris `infrastructure-type-filter.entity.ts`). Le VO `infrastructure-type-filter.vo.ts` contenait en revanche une vraie règle, minime mais réelle : `search?.trim() || undefined` — préservée dans la fonction de remplacement.

**Règle appliquée (retenue et documentée dans `crud-entity.pattern.json` v7, `design_decisions_v7`)** : le VO devient systématiquement une fonction (identité pure si aucune validation n'existe, ou portant la logique réelle sinon). L'Entity n'est conservée comme fichier séparé QUE si elle contient une transformation réelle au-delà du pass-through — c'est le cas d'`infrastructureFilterEntity` (règle métier réelle : `endDate` par défaut à la date du jour), ce n'est PAS le cas d'`infrastructure-type-filter.entity.ts` (aucune règle, uniquement une méthode `describe()` morte) : ce dernier fichier a donc été supprimé entièrement, pas converti en fonction passe-plat.

**Portée réelle de cette expérience** : `infrastructure-type` (create/update/delete/disable/enable/filter) migré — 6 fichiers Entity supprimés, 6 VO convertis en fonctions, cascade sur `infrastructure-type-repository.ts` (abstrait), `infrastructure-type-repository.impl.ts`, les 5 data-mappers (changement du type importé uniquement — accès déjà à plat, contrairement à `infrastructure` qui nécessitait aussi un retrait de préfixe `.data.`), et `InfrastructureTypeUseCase` (suppression de la chaîne `Vo.fromDto → Entity.fromVo → repository`, appel direct `repository.X(xVo(dto))`).

**Vérification** : `tsc -p tsconfig.app.json --noEmit` sur l'application entière après migration : 0 erreur. `eslint --fix --quiet` sur `administrative-infrastructure` : 0 erreur après `prettier --write`. Aucune référence résiduelle aux classes supprimées (`grep` scopé au module : 0 résultat).

**Ce que cette expérience NE couvre PAS** : `find-one` et `select` (Entity/VO de ces opérations) restent hors périmètre — non revus sous cet angle, la directive portant explicitement sur "create filtre delete modification". Sur ~14 modules réels de l'application, seuls `administrative-infrastructure` (les deux entités qu'il contient) et le module de référence `resources` suivent désormais ce schéma classless — les ~13 autres modules réels gardent leurs classes VO/Entity d'origine. L'instruction "dans toute l'application" reste donc partiellement exécutée : ce chantier est mécaniquement plus risqué qu'EXP-009 (un simple renommage de handler) car il touche des signatures de repository abstrait ET implémenté, plus un retrait de préfixe `.data.` non uniforme selon les modules — un codemod y est envisageable mais demande une conception et une vérification au moins aussi prudentes que celles d'EXP-009, module par module plutôt qu'en un seul passage automatique aveugle.

**Mise à jour du schéma** : `crud-entity.pattern.json` passe en v7 — `domain/entities/{ENTITY}/{ENTITY}-filter.entity.ts` retiré du cœur canonique obligatoire (n'est plus universel, sa présence dépend désormais d'un jugement de contenu — une transformation réelle existe ou non — que `check-pattern.js` ne peut pas vérifier par construction, cf. Charter §4 sur la séparation connaissance observée/déclarée). `{ENTITY}-create/update/delete.entity.ts` étaient déjà retirés depuis v6 mais non documentés formellement — corrigé ici.

## Correction méthodologique post-Expérience 010 — Contract/ValidateContract/Factory : distinguer le seam de validation du seam de construction

**Critique reçue, fondée** : l'exécution d'Expérience 010 a traité "l'Entity ne contient aucun champ/transformation supplémentaire par rapport au VO" comme équivalent à "toute la couche Contract/Vo/Factory est de la cérémonie" — sans jamais vérifier CE QUE la séparation Contract (souple) / ValidateContract (strict, via fonction d'assertion) / Factory (construction) protège réellement. C'est exactement le type d'exécution mécanique que ce projet est censé éviter depuis le Charter.

**Ce qu'une lecture réelle du code de `infrastructure` (create/update) a révélé** : le VO n'est pas un simple pass-through — il appelle `validateInfrastructureCreate(contract)` / `validateInfrastructureUpdate(contract)`, une vraie fonction d'assertion TypeScript (`asserts contract is XValidateContract`) qui lève `GenericRequiredError` si un champ obligatoire manque. C'est une garantie réelle, au niveau domaine, indépendante de la validation des formulaires Angular : si un appelant contourne un jour le formulaire (autre service, script, futur endpoint), c'est ce validateur — pas le formulaire — qui empêche une donnée invalide d'atteindre le repository/l'API.

**Mais une vérification plus poussée (pas seulement "la fonction existe et lève une erreur") a trouvé un vrai défaut, distinct, pré-existant** : la clé i18n passée à `GenericRequiredError` (ex. `ADMINISTRATIVE_INFRASTRUCTURE.INFRASTRUCTURE.FORM.ERROR.CREATE.NAME_REQUIRE`) n'existe dans aucune traduction (`src/assets/i18n/fr.json` ne contient aucune clé sous `FORM.ERROR.*` pour `INFRASTRUCTURE`). Pire : `GenericRequiredError.messageKey` est câblé en dur sur `'COMMON.ERROR.REQUIRED'`, qui n'existe pas non plus (seul `COMMON.ERROR.GENERIC` existe). Et `ErrorHandlerRegistry`/`UiFeedbackService.registerDefaultHandlers()` n'enregistre AUCUN handler pour `GenericRequiredError` — ni par classe ni par `code` (`COMMON_REQUIRED`). Conséquence vérifiée par lecture du code (pas supposée) : le validateur protège bien le repository contre une donnée invalide (l'exception est levée, l'appel s'arrête), mais si cette exception atteint `UiFeedbackService.notifyError()`, **aucun toast ne s'affiche** — la protection est réelle côté données, silencieuse côté utilisateur. Ce bug est antérieur à cette session, non introduit par elle, et reste hors périmètre de correction ici (chantier distinct : `ErrorHandlerRegistry`/traductions).

**Verdict sur la Factory (raisonné, pas choisi dans une liste)** : le seam de VALIDATION (Contract → assertion function → ValidateContract) est réel et ne doit jamais être aplati silencieusement — c'est la seule barrière domaine indépendante du formulaire. Le seam de CONSTRUCTION (Factory : ValidateContract → Entity) est, sur les deux instances réellement inspectées (`infrastructure`, `infrastructure-type`), vérifié comme n'ajoutant aucune garantie au-delà de ce que le type `ValidateContract` impose déjà statiquement — ni invariant, ni comportement, ni égalité, ni méthode vivante (les deux seules méthodes trouvées, `describe()` et `appliesToAdminScope()`, sont mortes). Appliquée à la propre règle déjà écrite par l'architecte (`extension_candidates` : une abstraction sans comportement actuel n'est retenue que si une annotation datée explique un scénario futur concret — absente ici), la Factory ne remplit pas ce critère. Le collapse Factory→appel direct du VO reste donc justifié, mais désormais par cette règle explicite, pas par un comptage de champs.

**Correctif appliqué (comble le gap identifié, "Combler le gap")** : `infrastructure-type` n'avait AUCUN Contract/ValidateContract/validateur pour `create`/`update` — contrairement à `infrastructure` dans le même module. Ajoutés : `infrastructure-type-{create,update}.contract.ts` (souple), `.validate-contract.ts` (strict), `.validator.ts` (fonction d'assertion, mêmes règles que le formulaire : name/description requis, uniqId requis pour update), sur le modèle exact d'`infrastructure`. Cascade : VO (appelle désormais le validateur), commands-mapper applicatif (Command → Contract au lieu de Command → Dto, suppression de deux casts `as string` non sûrs qui masquaient silencieusement un `undefined`), repository abstrait/impl, data-mappers, use-case. Défaut supplémentaire trouvé et corrigé au passage : `InfrastructureTypeFacade.create(user: any)` / `.update(user: any)` — la façade elle-même n'était pas typée du tout (contrairement à celle d'`infrastructure`), ce qui masquait totalement le trou de validation côté compilateur ; retypé sur `InfrastructureTypeCreateDto`/`UpdateDto` (élargis en champs optionnels pour rester cohérents avec la convention d'`infrastructure`, sauf `uniqId` d'update qui reste requis, à l'identique d'`infrastructure`). Vérifié : `tsc --noEmit` 0 erreur, `eslint` 0 erreur.

**Limite explicite** : ce correctif ne couvre que `create`/`update` d'`infrastructure-type` (`delete`/`enable`/`disable` restent des identités pures, à l'identique d'`infrastructure`, qui n'a pas non plus de validateur pour ces trois opérations — convention cohérente, pas un oubli). Le bug `GenericRequiredError`/`ErrorHandlerRegistry` reste ouvert et non corrigé, documenté ici pour ne pas être perdu.

## Expérience 011 — Critère mécanique retenu : `Validators.required` dans le formulaire détermine où le garde-fou domaine est justifié

**Critique reçue, qui corrige la méthode d'Expérience 010 et de sa correction** : identifier au cas par cas si un `Contract`/`ValidateContract`/validateur est justifié pour une entité donnée ne doit pas dépendre d'un jugement ad hoc. L'architecte a fourni un critère mécanique, vérifiable par grep, déjà implicite dans tout le travail précédent : **si le formulaire Angular d'une entité déclare des champs `Validators.required`, alors un garde-fou domaine (Contract souple → assertion function → ValidateContract strict) est justifié pour cette entité**, indépendamment de la question distincte "Factory ou pas" (tranchée en Expérience 010 : la Factory est éliminée quand elle n'ajoute aucune transformation réelle, ce qui reste vrai ici).

**Audit réel, mené par grep sur les 17 entités de l'application ayant une opération `create`** (`find ... -iname "*-create.dto.ts"` puis `grep -rl "Validators.required" par entité) : AVANT cette expérience, seules 3 entités sur 17 avaient un garde-fou domaine réel (`infrastructure`, `infrastructure-type` — ajoutée en Expérience 010 — et `messaging`). Les 14 autres ont des champs `Validators.required` bien réels dans leur formulaire mais AUCUN Contract/ValidateContract/validateur domaine : `departments`, `regions`, `municipalities` (administrative-boundary — le module qui a servi de source au schéma canonique lui-même) ; `home`, `terms-use`, `slide`, `privacy-policy`, `legal-notice`, `news` (content-management) ; `tasks-actions` (processing) ; `users`, `profiles-permissions` (settings-security) ; `participants`, `teams` (team-organization).

**Traité dans cette expérience : `departments`, `regions`, `municipalities` (administrative-boundary), en entier.** Pour chacune : `{ENTITY}-{create,update}.contract.ts` (souple) + `.validate-contract.ts` (strict) + `.validator.ts` (fonction d'assertion, champs alignés exactement sur ceux marqués `Validators.required` dans `{ENTITY}-form.store.ts` — pas plus, pas moins) créés ; VO converti en fonction appelant le validateur (suppression simultanée de l'Entity/Factory create/update, qui ne faisait aucune transformation réelle, même critère qu'Expérience 010) ; cascade sur repository abstrait/impl, data-mappers, commands-mappers applicatifs, use-case.

**Bug réel trouvé et corrigé au passage, distinct pour les trois entités** : `population` et `infrastructure` sont des champs NUMÉRIQUES où `0` est une valeur légitime. Les data-mappers utilisaient `if (create.population) { ... }` — un test de vérité (falsy check) qui aurait silencieusement supprimé une valeur `0` réelle avant l'envoi à l'API. Corrigé : une fois `population`/`infrastructure` validés comme présents (non `undefined`/`null`) par le validateur, le mapper les assigne sans condition. C'est le même type de bug que celui trouvé sur `InfrastructureTypeFacade.create(user: any)` en Expérience 010 — un défaut réel, pas la conséquence de ce refactor, révélé par lui.

**Vérifié** : `tsc -p tsconfig.app.json --noEmit` sur l'application entière : 0 erreur. `eslint --fix` sur `administrative-boundary` : 0 erreur. Aucune référence résiduelle aux classes supprimées (grep scopé au module : 0 résultat).

**Ce qui reste, explicitement, pas de côté** : les 11 entités restantes (`home`, `terms-use`, `slide`, `privacy-policy`, `legal-notice`, `news`, `tasks-actions`, `users`, `profiles-permissions`, `participants`, `teams`) ont le même défaut mesuré (`Validators.required` réel, aucun garde-fou domaine) et suivent la même méthode — non traitées dans cette expérience faute de temps, pas parce que le critère ne s'y appliquerait pas. Tâches ouvertes dans le suivi de session.

## Expérience 012 — Revue systémique du module `administrative-infrastructure` (vue d'ensemble, pas fichier par fichier)

**Directive reçue** : rester concentré sur `administrative-infrastructure`, raisonner à l'échelle du module entier (pas fichier par fichier), corriger les bugs réels, apporter une amélioration d'ingénierie de fond, et mettre à jour les documents de gouvernance en conséquence pour évaluer la clôture d'Étape 1 sur ce module.

**Méthode** : cartographie complète du module (`find` exhaustif, ~230 fichiers), comparaison structurelle systématique entre les deux entités (`infrastructure`, `infrastructure-type`) plutôt qu'une correction isolée, avant toute nouvelle modification.

**Bug critique trouvé (régression non détectée)** : `application/use-cases/infrastructure/infrastructure.use-case.ts` référençait encore, en code réel, des fichiers supprimés lors d'une passe antérieure de cette même session (`infrastructure-delete.entity.ts`, `infrastructure-create.factory.ts`, `infrastructure-update.factory.ts`, la classe `InfrastructureDeleteVo`) — `tsc --noEmit` frais confirmait 4 erreurs `Cannot find module`. Ce fichier n'avait, en réalité, jamais été corrigé malgré une affirmation contraire dans un tour précédent de cette session — c'est très exactement l'exemple que l'architecte a montré ("`const vo = infrastructureCreateVo(dto); const entity = infrastructureCreateFactory(vo);`") pour illustrer une exécution non vérifiée. Corrigé et revérifié par une exécution fraîche de `tsc`, pas une simple relecture.

**Deux slices de code mort confirmées par grep (aucune référence hors de leur propre fichier de définition, aucun template)** :
- `responsibilities-select`, copie complète dans `infrastructure/` (entity, repository abstrait+impl, mapper, api, use-case, providers DI) — jamais câblée dans l'agrégateur DI principal, et son propre `use-case` importait par erreur les dépendances d'`infrastructure-type/` (confusion de copier-coller). Supprimée (8 fichiers).
- `responsibilities-select`, version `infrastructure-type/` — câblée au DI principal, mais sa `Facade` n'est consommée par aucun composant ni template de toute l'application (grep global, pas seulement le module). Supprimée également.

**Une troisième slice signalée, PAS supprimée unilatéralement** : `infrastructure-select` (facade + use-case + repository + mapper + entity + providers DI, 9 fichiers) — câblée au DI, zéro consommateur trouvé dans toute l'application par grep global. Contrairement à `responsibilities-select`, rien n'indique une erreur évidente (pas d'import vers le mauvais module) — cela pourrait être une fonctionnalité construite en avance d'une UI pas encore branchée. Décision laissée explicitement à l'architecte plutôt que tranchée seule.

**Bug de robustesse trouvé et corrigé (pipeline d'erreur)** : les validateurs de domaine (fonctions d'assertion) lèvent une exception **synchrone**. Or `{ENTITY}-create.bus.ts`/`-update.bus.ts` appellent leur handler de façon synchrone, sans `try/catch` ni `defer()` — une erreur de validation ne peut alors JAMAIS atteindre le `catchError` de la façade ni `UiFeedbackService.notifyError()` : elle remonte comme une exception JS non interceptée, invisible pour l'utilisateur. Corrigé en enveloppant `create()`/`update()` des deux use-cases (`infrastructure`, `infrastructure-type`) dans `rxjs.defer()`, qui différe la construction de l'Observable (donc l'appel à la fonction VO qui valide) jusqu'à la souscription — RxJS convertit alors automatiquement toute exception survenue à ce moment-là en erreur Observable, correctement interceptée en aval. **Ce même défaut existe dans tous les autres modules utilisant ce patron** (`administrative-boundary`, ajouté cette session ; `communication/messaging`, préexistant) — non corrigé là, explicitement hors périmètre de cette passe module-par-module, signalé pour suite.

**Bug d'affichage utilisateur trouvé et corrigé** : `GenericRequiredError` n'avait aucun handler enregistré dans `UiFeedbackService.registerDefaultHandlers()` (fichier partagé, `src/shared`) — une erreur de validation qui atteignait malgré tout le pipeline (après le correctif `defer()`) restait silencieuse côté utilisateur, aucun toast ne s'affichant. Handler ajouté, même convention que les autres `DomainError`. Les clés i18n réellement utilisées par les validateurs d'`administrative-infrastructure` n'existaient dans aucune traduction (`fr.json` ne contenait aucune clé sous `FORM.ERROR.*` pour `INFRASTRUCTURE`/`INFRASTRUCTURE_TYPE`) — ajoutées. Les mêmes clés pour `administrative-boundary` (ajoutées à la session précédente) restent manquantes — explicitement hors périmètre ici, signalé.

**Cohérence structurelle** : `domain/entities/{ENTITY}/{ENTITY}-find-one-filter.entity.ts` — même verdict qu'Expérience 010 pour `{ENTITY}-filter.entity.ts` (wrapper à champ unique, aucune transformation réelle) — converti en fonction pour les deux entités, cascade repository/mapper/use-case. Retiré du cœur canonique (`crud-entity.pattern.json` v8).

**Vérifié** : `tsc -p tsconfig.app.json --noEmit` sur l'application entière : 0 erreur. `eslint --fix` sur `administrative-infrastructure` + `ui-feedback.service.ts` : 0 erreur. Aucune référence résiduelle aux classes/fichiers supprimés (grep scopé au module : 0 résultat).

**Verdict honnête sur la clôture d'Étape 1 pour ce module** : le socle CRUD (create/update/filter/find-one-filter classless, Contract+validateur réel sur create/update, mappers homogènes, pas de code mort connu restant sauf la question `infrastructure-select` non tranchée) est cohérent et vérifié entre les deux entités. Ce qui reste explicitement hors de ce périmètre et non résolu : `find-one`/`select` (Entities conservées à raison, logique réelle via `with()`) ; asymétrie de nommage `{ENTITY}-repository.impl.ts` vs `{ENTITY}.repository.impl.ts` (schéma) ; `presentation/{ENTITY}` vs `presentation/features/{ENTITY}` (déviation déjà connue, partagée avec tout le reste de l'application) ; le défaut `defer()` et les clés i18n manquantes dans les AUTRES modules (non corrigés ici par discipline de périmètre). Étape 1 peut être considérée close pour la partie CRUD/validation de ce module spécifiquement — pas pour l'homogénéité de nommage/dossier, qui reste un chantier distinct et partagé avec toute l'application.

## Règle ajoutée — traiter "pourrait servir plus tard" comme une hypothèse falsifiable, pas une justification

Constat : un fichier sans comportement actuel (ex. `ValidateContract` sans validation) peut toujours être défendu par "il pourrait en avoir dans une future feature ou un autre module". Cet argument, tel quel, est infalsifiable — il justifierait n'importe quelle indirection n'importe où, donc il ne discrimine rien. Règle retenue : une abstraction actuellement sans comportement n'est admissible au cœur canonique que si elle porte une annotation explicite et datée (connaissance déclarée, jamais déduite) précisant le scénario futur concret attendu, le module/feature qui en aurait besoin, et une date de révision. Sans cette annotation, l'abstraction reste hors du cœur — pas supprimée du code, simplement pas exigée pour les futures générations SEOS tant qu'elle n'est pas promue. Voir `seos/patterns/crud-entity.pattern.json`, section `extension_candidates`, pour l'application concrète de cette règle à `commands-mappers`/`contracts`/`factories`.

## Correction du socle (suite à l'audit des 11 modules)

`crud-entity.pattern.json` a été corrigé : 4 fichiers déplacés d'`application/store/` vers `presentation/store/`, car ce dernier est la convention réellement majoritaire (9/11 modules) alors que le schéma initial suivait `administrative-boundary`, minoritaire sur ce point précis (3/11). Conséquence, vérifiée et assumée : `administrative-boundary` (source initiale du schéma) passe à 95,4 % de conformité à son propre schéma corrigé (83/87), et `administrative-infrastructure` remonte à 87,4 % (76/87). Aucun des deux modules n'est à 100 % — c'est le résultat attendu d'un schéma qui reflète désormais la majorité réelle plutôt qu'un seul module source.

## Étape 1 close — module de référence "resources"

Livré : `src/presentation/pages/seos-reference` (87 fichiers), généré par `seos/tools/generate-reference-module.js`, non branché dans les routes de l'app (aucun risque sur le build réel). Vérifié à trois niveaux, pas seulement structurellement :

- `tsc -p tsconfig.app.json --noEmit` : 0 erreur.
- `eslint --quiet` : 0 erreur (après correction de 5 variables inutilisées et d'une classe vide détectées au premier passage).
- `seos/tools/check-pattern.js` : 100 % (87/87), attendu puisque ce module a servi à fixer le schéma corrigé v3.

Décisions de conception verrouillées dans `crud-entity.pattern.json` (v3) : `presentation/store` (déjà acté v2) ; `presentation/features/{ENTITY}/...` préféré à `presentation/{ENTITY}/...` (majoritaire, 6/11, et organisationnellement plus propre) ; `domain/controls` inclus après vérification que le code y est actif dans 5 des 6 modules qui le possèdent (les 4 fichiers morts trouvés dans `administrative-boundary` sont un résidu de refactor localisé à ce module, pas une preuve contre le pattern) ; `domain/services` (form-helper) et `domain/validators` (mutualisé au niveau module) inclus ; couche `commands-mappers/contracts/factories` toujours exclue du cœur (non promue, cf. règle de falsifiabilité ci-dessus).

> **Correction (Expérience 013, v9)** : le verdict `domain/controls` ci-dessus est invalidé — il vérifiait le mauvais dénominateur ("5 des 6 modules qui le possèdent" au lieu de "combien sur la totalité des entités CRUD de l'application"). Mesure correcte : 4 fichiers existent dans toute l'application (tous dans `administrative-boundary`) et les 4 sont entièrement commentés. Voir Expérience 013 ci-dessous.

## Expérience 013 — Deuxième passe méta sur `administrative-infrastructure` : trancher schéma vs architecture, bucket par bucket

**Directive reçue** : à partir de la sortie brute de `check-pattern.js` (75/88, 13 fichiers manquants), ne pas traiter les manques fichier par fichier ni les déférer — grouper en buckets et, pour chaque bucket, trancher explicitement : le schéma (`crud-entity.pattern.json`) est-il faux, ou l'architecture réelle a-t-elle un vrai trou ? Puis exécuter la décision, pas seulement la formuler.

**Méthode** : Règle 0 systématique par bucket — mesurer la fraction réelle d'entités CRUD app-wide qui possèdent chaque fichier du cœur (jamais seulement la fraction des modules qui le possèdent déjà), et lire le contenu des fichiers existants pour vérifier s'ils sont vivants ou morts, avant toute décision.

**Bucket 1 — `domain/controls/{ENTITY}/{ENTITY}-{filter,form}.control.ts` : schéma corrigé.** Décision de conception v3 invalidée (voir correction ci-dessus, section "Étape 1 close"). Mesure correcte : 11/65 et 8/65 entités app-wide seulement — et les 4 fichiers réels existant à ce chemin exact dans toute l'application (`administrative-boundary/domain/controls/{departments,municipalities}/...`) sont **entièrement commentés**, confirmé par lecture de contenu, pas par comptage. `administrative-infrastructure` a son propre contrôle de formulaire/filtre, actif, mais à un autre chemin (`presentation/store/{ENTITY}/{ENTITY}-{filter,form}.control.ts`, déjà au cœur canonique, distinct). Verdict : le schéma documentait un résidu de refactor mort localisé à un seul module comme s'il était un pattern universel. Retiré de `core_files` (v9). Aucune modification de code.

**Bucket 2 — `domain/services/{ENTITY}/{ENTITY}-form-helper.service.ts` : schéma corrigé (mauvais chemin, pas mauvais fichier).** Lecture du contenu réel (`InfrastructureFormHelperService`) : dépend de `Router` et `MessageService` (Angular) — un contenu 100 % présentation, qui violerait Clean Architecture s'il vivait réellement dans `domain/`. Convention majoritaire app-wide : 17 fichiers sous `presentation/` contre 13 sous `domain/services/`. Chemin corrigé dans le schéma vers `presentation/features/{ENTITY}/{ENTITY}-form/{ENTITY}-form-helper.service.ts` (v9). Aucune modification de code — seul le schéma décrivait le mauvais endroit.

**Bucket 3 — `domain/interfaces/{ENTITY}/{ENTITY}-find-one-props.interface.ts` : code aligné sur la convention majoritaire.** Convention vérifiée : 11/19 entités avec `find-one` (58 %) extraient déjà cette interface dans son propre fichier plutôt que de la déclarer en ligne dans `{ENTITY}-find-one.entity.ts`. `infrastructure` et `infrastructure-type` étaient minoritaires (interface en ligne). Alignés : extraction dans deux nouveaux fichiers, entités converties en simple import, mappers en aval mis à jour (leur import pointait vers l'entité, cassé sinon). Changement de code réel, aucun changement de comportement.

**Bucket 4 — `domain/contracts/{ENTITY}/{ENTITY}-filter.contract.ts` + `domain/validators/{ENTITY}/{ENTITY}-filter.validator.ts` : clarification du critère, pas un gap.** `infrastructure-type` ne les a pas ; `check-pattern.js` les signale comme manquants. Vérification du contenu des 9 occurrences réelles existantes (`infrastructure`, `messaging`, `notifications`, `resources`, `approve/close/evaluate/reject/download`) : toutes valident **exactement** la même règle — `startDate.getTime() > endDate.getTime()` → `DateRangeInvalidError`, une cohérence inter-champs sur une plage de dates. Aucune n'est liée à un champ obligatoire (`Validators.required` ne s'applique jamais à un filtre). Or le filtre d'`infrastructure-type` n'a ni `startDate` ni `endDate` (`search`, `profile`, `role`, `isActive` uniquement). Conclusion : le critère mécanique réel n'est pas "toute entité CRUD doit avoir un filter.contract", mais "le filtre de cette entité expose-t-il un couple startDate/endDate ?". En créer un pour `infrastructure-type` serait de la cérémonie sans comportement — même verdict que la règle VO/Entity de l'Expérience 010. Décision : ni le schéma ni le code ne changent ; `core_files` documente désormais explicitement la condition (v9, `clarification_filter_contract_validator_conditionnel`). `check-pattern.js` continuera de signaler ces 2 fichiers comme manquants pour `infrastructure-type` — c'est un flag structurel correct et attendu, pas une régression, conséquence assumée de la limite structurelle-only du script (Research-Charter section 4).

**Vérifié** : `tsc -p tsconfig.app.json --noEmit` sur l'application entière : 0 erreur. `eslint --fix` sur `administrative-infrastructure` : 0 erreur. `check-pattern.js` (mesure initiale, alors non re-vérifiée à froid) : `infrastructure` 85,2 % → 87,2 % ; `infrastructure-type` 82,8 % (mesure brute utilisateur) → 84,9 %. **Correction (Expérience 014)** : ces deux chiffres se sont révélés faux — une lecture disque transitoire du sandbox (déjà rencontrée cette session sous forme de plantages bash) faisait apparaître comme "manquants" des fichiers de présentation pourtant bien présents sur disque depuis longtemps (`infrastructure-form-helper.service.ts`, les composants `-list`/`-page`, `.routes.ts`, `-paths.constants.ts`). Deux exécutions fraîches consécutives de `check-pattern.js` donnent des chiffres stables et corrects : voir Expérience 014. `crud-entity.pattern.json` passé en v9.

**Ce qui reste ouvert, explicitement posé à l'architecte plutôt que tranché seul** : (1) `infrastructure-select` (9 fichiers, câblé DI, zéro consommateur app-wide, aucune preuve d'erreur évidente contrairement à `responsibilities-select`) — supprimer ou garder comme fondation WIP ? (2) `infrastructure` a des stubs orphelins jamais câblés (`InfrastructureEnableCommand`, `infrastructure-{enable,disable}.dto.ts`, sans handler/bus/facade), alors qu'`infrastructure-type` a l'enable/disable entièrement câblé de bout en bout — fonctionnalité à compléter pour la parité, ou stubs à retirer si jamais prévus pour cette entité ? Question produit, pas une question d'ingénierie pure.

## Expérience 014 — Vérification de l'hypothèse "mauvais dossier" pour helper/validation-service (infrastructure, infrastructure-type) : code mort, pas un mauvais chemin

**Hypothèse reçue** : l'architecte pense que `{ENTITY}-form-helper.service.ts` et `{ENTITY}-form-validation.service.ts` d'`infrastructure`/`infrastructure-type` ne sont pas dans le bon dossier en termes de responsabilité.

**Méthode** : lecture du contenu réel des deux fichiers `*-form-validation.service.ts`, et grep app-wide de leurs classes (`InfrastructureFormValidationService`, `InfrastructureTypeFormValidationService`) et de leurs méthodes (`displaySubmitError`, `showValidationErrors`) pour trouver leurs consommateurs réels, avant toute conclusion sur le "bon dossier".

**Résultat, contraire à l'hypothèse de départ** : ce n'est pas un problème d'emplacement — les deux fichiers `{ENTITY}-form-validation.service.ts` sont des **doublons quasi identiques** (mêmes deux méthodes, `getErrorMessage`/`showValidationErrors`, seules les clés i18n changent) du service déjà partagé `@shared/domain/services/form-validation.service.ts` (`FormValidationService`, générique, clés `COMMON.VALIDATION.*`). Grep app-wide confirme : `InfrastructureFormValidationService` et `InfrastructureTypeFormValidationService` ont **zéro consommateur**, nulle part — ni composant, ni provider. Lecture de `infrastructure-form.component.ts` et `infrastructure-type-form.component.ts` : les deux injectent et utilisent réellement le `FormValidationService` **partagé**, jamais leur propre service local. Même verdict pour la méthode `displaySubmitError()` des deux `{ENTITY}-form-helper.service.ts` : définie, jamais appelée par aucun composant (les deux formulaires gèrent l'échec de soumission via l'état `actionState`/`actionSuccess` de la façade, pas via cette méthode).

**Décision et exécution** : suppression des deux fichiers `{ENTITY}-form-validation.service.ts` (code mort à 100 %, même catégorie que `responsibilities-select` en Expérience 012 — dupliqué, zéro consommateur). Suppression de la méthode morte `displaySubmitError()` (et de l'import `MessageService`/`TranslateService` devenu inutile) dans les deux `{ENTITY}-form-helper.service.ts`, qui restent sinon utilisés et corrects (navigation + libellés SweetAlert, seules méthodes réellement appelées). `presentation/features/{ENTITY}/{ENTITY}-form/{ENTITY}-form-helper.service.ts` reste donc au bon endroit (déjà tranché Expérience 013) — l'hypothèse "mauvais dossier" était fausse, la vraie anomalie était un doublon mort à côté, pas un fichier mal rangé.

**Effet de bord découvert pendant la vérification** : les chiffres `check-pattern.js` rapportés dans l'Expérience 013 (`infrastructure` 87,2 %, `infrastructure-type` 84,9 %) provenaient d'une lecture disque transitoire du sandbox (des fichiers de présentation existant réellement depuis longtemps — `infrastructure-form-helper.service.ts`, composants `-list`/`-page`, `.routes.ts`, `-paths.constants.ts` — apparaissaient à tort comme absents). Deux exécutions fraîches et consécutives, après cette passe, donnent des chiffres stables : `infrastructure` 81/86 (94,2 %), `infrastructure-type` 79/86 (91,9 %). Chiffres corrigés dans l'Expérience 013 et dans `crud-entity.pattern.json` (`design_decisions_v9.verification`). Note méthodologique retenue : ne jamais figer un chiffre `check-pattern.js` sans au moins deux exécutions consécutives concordantes, surtout juste après une série d'écritures fichier.

**Vérifié** : `tsc -p tsconfig.app.json --noEmit` : 0 erreur. `eslint --fix` sur `administrative-infrastructure` : 0 erreur. Grep app-wide post-suppression : 0 référence résiduelle aux deux classes/fichiers supprimés. `check-pattern.js` (à ce moment-là) : `infrastructure` 81/86 (94,2 %), `infrastructure-type` 79/86 (91,9 %) — stable sur deux exécutions consécutives à ce moment. **Correction ultérieure (Expérience 016)** : "stable sur deux exécutions" s'est révélé insuffisant comme critère — une 3ᵉ exécution, plus tard, a donné un résultat différent pour `infrastructure`. Vérification directe par `find` (indépendante de `check-pattern.js`) : 4 des 5 fichiers alors rapportés "manquants" (`infrastructure-select-response-api.dto.ts`, `infrastructure-find-one.repository.impl.ts`, `infrastructure-select.repository.impl.ts`, `infrastructure.repository.impl.ts`) existaient réellement sur disque depuis le début. Chiffre réel confirmé au sol : `infrastructure` 84/86 (97,7 %). Le sandbox a donc produit, sur cette même session, deux types d'erreurs de lecture opposés (faux manquants, puis faux présents) — la règle "deux exécutions concordantes suffisent" est elle-même invalidée ; seule une vérification directe par `find`/`ls`, indépendante de l'outil, fait foi quand le chiffre importe.

## Expérience 015 — Auto-correction : le regroupement navigation + libellés SweetAlert dans `{ENTITY}-form-helper.service.ts` n'est PAS une violation SRP

**Contexte** : après l'Expérience 014, j'avais signalé au passage un "bémol" — le nom générique "helper" regroupant `navigateToXList()` et `getSweetAlertTitle/Message()` me semblait être un fourre-tout à décomposer plus finement. L'architecte a demandé de vérifier et corriger avec la même rigueur que d'habitude plutôt que d'exécuter une opinion non vérifiée.

**Méthode** : appliquer Rule 0 à ma propre affirmation avant de coder quoi que ce soit — vérifier si cette "violation SRP" est un vrai défaut local à `infrastructure`/`infrastructure-type`, ou si c'est la forme réelle et stable du pattern partout ailleurs dans l'application.

**Résultat** : après nettoyage du code mort (Expérience 014, suppression de `displaySubmitError`), `InfrastructureFormHelperService` ne contient plus que `navigateToInfrastructureList()` (dépendance réelle à `Router`) et `getSweetAlertTitle/Message()` (fonctions pures, aucune dépendance injectée). Comparaison avec `DepartmentsFormHelperService` (`administrative-boundary`, lignée source du schéma) et `MessagingFormHelperService` (`communication`, lignée totalement indépendante) : **structure identique à l'octet près** — un seul `Router` injecté, les deux mêmes méthodes de sélection de clé i18n, aucune segmentation supplémentaire, dans deux lignées qui n'ont jamais partagé de code entre elles. Deux lignées indépendantes convergeant vers exactement la même forme est un signal fort (Rule 0, critère de convergence indépendante) que ce regroupement est le vrai pattern voulu — "orchestration post-soumission de ce formulaire précis" est une seule responsabilité cohérente, pas un fourre-tout.

**Verdict** : mon "bémol" précédent était une opinion esthétique non vérifiée, pas une conclusion d'ingénierie. Rétracté. Aucune modification de code — scinder `infrastructure`/`infrastructure-type` à l'écart de ce pattern aurait introduit une nouvelle déviation solitaire, exactement le type d'erreur corrigé tout au long de cette session (s'aligner sur la convention réelle, pas s'en écarter sans preuve).

**Trouvaille distincte, réelle, mais explicitement hors périmètre** : `getSweetAlertTitle`/`getSweetAlertMessage` sont dupliquées quasi à l'identique dans au moins 3 lignées (`administrative-boundary`, `communication`, `administrative-infrastructure` ×2) — seul le préfixe de namespace i18n change (`ADMINISTRATIVE_INFRASTRUCTURE.INFRASTRUCTURE.SWEET_ALERT...` vs `COMMUNICATION.MESSAGING.SWEET_ALERT...`). C'est une vraie duplication DRY à l'échelle de l'application (une fonction générique `buildSweetAlertKeys(namespace, isEditMode)` l'éliminerait), mais la corriger toucherait des modules hors du périmètre fixé ("reste focus sur administrative-infrastructure") — signalé pour une passe transversale future, non exécuté ici.

**Vérifié** : aucune modification de code dans cette expérience ; conclusion basée sur lecture de contenu comparée (3 fichiers, 2 lignées indépendantes).

## Expérience 016 — Résolution de la question ouverte "infrastructure" enable/disable : décision produit + code mort confirmé

**Contexte** : question laissée ouverte dans l'Expérience 012/013 ("infrastructure a des stubs orphelins jamais câblés... fonctionnalité à compléter pour la parité, ou stubs à retirer ?"). L'architecte tranche : `infrastructure` ne doit pas avoir de statut actif/inactif, et demande de retirer la vérification `check-pattern.js` correspondante (`domain/enums/infrastructure/infrastructure-status.enum.ts`, `infrastructure-status.mapper.ts` signalés manquants).

**Vérification avant action** : `domain/enums/{ENTITY}/{ENTITY}-status.enum.ts` et `.../​{ENTITY}-status.mapper.ts` sont un pattern réellement majoritaire à l'échelle de l'app — 24 fichiers `-status.enum.ts` et 21 fichiers `-status.mapper.ts`, répartis sur une dizaine de modules indépendants (`administrative-boundary`, `communication`, `content-management`, `report-states`, `requests`, `seos-reference`, `settings-security`, `team-organization`). Retirer ces deux entrées de `core_files` **globalement** aurait été une erreur — cela aurait cassé la vérification pour ~20 entités qui ont réellement ce cycle de vie, dont `infrastructure-type`, la propre entité sœur d'`infrastructure` dans le même module (câblage complet confirmé : command/bus/handler/mapper/vo/api-dto).

**Décision exécutée** : pas de suppression globale du schéma — clarification ajoutée (v10) documentant que ces 2 fichiers restent `core_files` par défaut, mais que `infrastructure` (et seulement cette entité) est une exception produit assumée : elle ne supporte pas enable/disable, contrairement à `infrastructure-type`. `check-pattern.js` continuera de signaler ces 2 fichiers comme manquants pour `infrastructure` — flag structurel attendu, pas une régression, pas un gap.

**Conséquence directe, exécutée dans la foulée** : les 4 fichiers orphelins d'`infrastructure` (`application/dto/infrastructure/infrastructure-{enable,disable}.dto.ts`, `application/commands/infrastructure/infrastructure-{enable,disable}.command.ts`) n'ont plus aucune raison d'exister — confirmés morts par grep app-wide (zéro référence, y compris par chemin d'import) avant suppression. Un bug distinct trouvé au passage : `infrastructure-disable.command.ts` (dossier `infrastructure/`) déclarait une classe nommée `InfrastructureTypeDisableCommand` — mauvais nom, résidu de copier-coller jamais corrigé ni utilisé, preuve supplémentaire que ce départ de fonctionnalité n'avait jamais été terminé ni relu. Les 4 fichiers supprimés.

**Vérifié** : `tsc -p tsconfig.app.json --noEmit` : 0 erreur. Grep app-wide post-suppression sur les 4 noms de classes et les 4 chemins d'import : 0 référence résiduelle. `check-pattern.js` + vérification indépendante par `find` (voir correction dans la note de l'Expérience 014 ci-dessus sur la fiabilité du sandbox) : `infrastructure` 84/86 (97,7 %), les 2 seuls fichiers manquants étant exactement `infrastructure-status.enum.ts` et `infrastructure-status.mapper.ts` — l'exception produit actée dans ce paragraphe, aucun autre gap réel.

## Expérience 017 — Retrait définitif et global de `status.enum`/`status.mapper` et `form-helper.service` du cœur canonique

**Contexte** : l'architecte revient sur la décision de l'Expérience 016. Là où j'avais documenté `infrastructure` comme une exception ponctuelle à un pattern resté `core_files` (parce que majoritaire, ~24/~21 fichiers), l'architecte demande explicitement le retrait **global et définitif** de `status.enum`/`status.mapper` du cœur canonique — pas seulement une exception documentée pour une entité — et le même traitement pour `{ENTITY}-form-helper.service.ts`.

**Différence avec les corrections précédentes de cette session** : jusqu'ici, chaque retrait de `core_files` (domain/controls en v9, filter.contract conditionnel en v9) était justifié par Rule 0 — preuve que le pattern n'est en réalité pas majoritaire, ou pas universellement applicable. Ce n'est pas le cas ici : `status.enum`/`status.mapper` restent un pattern réellement répandu (Expérience 016), et `form-helper.service.ts` avait été confirmé, par convergence indépendante entre deux lignées (Expérience 015), comme la forme réelle et stable du pattern partout où il existe. Ce retrait n'est donc pas une correction Rule 0 mais une **décision de conception assumée** : la prévalence d'un fichier ne le rend pas automatiquement obligatoire pour toute future entité CRUD générée par SEOS — "cycle de vie actif/inactif" et "confirmation SweetAlert + retour à la liste" sont des besoins métier propres à certaines entités, pas des briques structurelles du CRUD lui-même (create/read/update/delete/filter).

**Exécuté** : `domain/enums/{ENTITY}/{ENTITY}-status.enum.ts`, `infrastructure/data/mappers/{ENTITY}/{ENTITY}-status.mapper.ts` et `presentation/features/{ENTITY}/{ENTITY}-form/{ENTITY}-form-helper.service.ts` retirés de `core_files` (v11) et déplacés vers `extension_candidates` (`status_enum_mapper`, `form_helper_service`), avec la preuve de prévalence déjà établie conservée pour traçabilité — rien n'est perdu, seul le statut "obligatoire" est levé. `design_decisions_v10` (l'exception ponctuelle `infrastructure`) est conservé pour l'historique mais explicitement marqué "supersédé" : l'exception qu'il documentait n'a plus d'objet puisque le pattern n'est plus obligatoire pour personne.

**Aucun fichier de code touché** — seul `crud-entity.pattern.json` change. Les fichiers `status.enum`/`status.mapper`/`form-helper.service.ts` qui existent déjà (infrastructure-type, departments, messaging, etc.) restent en place, inchangés ; ils cessent seulement d'être *exigés* pour toute nouvelle entité.

**Vérifié** : `crud-entity.pattern.json` reste un JSON valide. `check-pattern.js` recalculé sur un dénominateur réduit (83 au lieu de 86) : `infrastructure` passe à **100 % (83/83)** — plus aucun fichier manquant. `infrastructure-type` passe à 77/83 (92,8 %), les 6 manquants restants étant tous déjà connus et documentés avant cette expérience (filter.contract/validator — exception conditionnelle Expérience 013 ; select-response-api.dto.ts et 3 asymétries de nommage `-repository.impl.ts` — déjà signalés en Expérience 012/013, hors périmètre).

## Expérience 018 — Ouverture de l'Étape 2 : validation sémantique (`seos/tools/check-semantics.js`)

**Directive reçue** : passer à l'Étape 2. Clarification obtenue : `check-pattern.js` (Étape 1) ne vérifie que la PRÉSENCE des fichiers ; l'Étape 2 doit vérifier leur CONTENU. Périmètre initial validé par l'architecte : 4 règles mécaniques, chacune dérivée d'un bug réel déjà trouvé et corrigé manuellement cette session — pas des règles inventées a priori.

**Outil livré** : `seos/tools/check-semantics.js`, même interface que `check-pattern.js` (`node check-semantics.js <module> <entite>`), zéro dépendance externe, zéro IA — analyse textuelle (regex + comptage de parenthèses/accolades équilibrées), dans le même esprit déterministe que le reste de SEOS.

**Les 4 règles et leur origine** :
1. **`defer()` sur `create()`/`update()`** — le use-case doit envelopper l'appel repository dans `rxjs.defer()`, sinon un throw synchrone du validateur de domaine ne remonte jamais au pipeline d'erreur. Bug réel trouvé et corrigé Expérience 008/012.
2. **Clés i18n des validateurs présentes dans `fr.json`** — bug réel trouvé et corrigé Expérience 008/012 (`GenericRequiredError` silencieuse faute de clé).
3. **Toute classe `extends DomainError` doit avoir un handler dans `UiFeedbackService.registerDefaultHandlers()`** — même famille de bug que le point 2, cause différente (clé présente mais aucun handler enregistré).
4. **Champs `Validators.required` du formulaire Angular ↔ champs vérifiés par le validateur de domaine (`{ENTITY}-create.validator.ts`)** — le critère mécanique donné explicitement par l'architecte en Expérience 011, désormais vérifiable automatiquement plutôt qu'appliqué à la main.

**Résultat immédiat, avant même la fin de l'écriture de l'outil** : en construisant la règle 4 à la main sur `infrastructure`/`infrastructure-type` pour calibrer le regex, j'ai trouvé un vrai bug que je n'avais pas détecté en écrivant moi-même ces validateurs plus tôt dans la session : `infrastructure-create.validator.ts` exige `name` et `type`, mais `infrastructure-form.store.ts` ne marquait `Validators.required` que sur `description`/`position` — `name`/`type` n'avaient AUCUNE validation Angular. Même chose pour `infrastructure-type` (`name` exigé côté domaine, absent côté formulaire). Conséquence concrète : un utilisateur pouvait soumettre un formulaire jugé valide par Angular, puis recevoir une erreur de validation seulement à la soumission — exactement le genre de défaut que la règle 4 est faite pour attraper. Corrigé : `Validators.required` ajouté sur les 3 champs concernés (2 fichiers `-form.store.ts`). Re-vérifié : `check-semantics.js` confirme désormais 0 problème sur la règle 4 pour les deux entités.

**Trouvaille app-wide, réelle, mais hors périmètre de correction immédiate** : la règle 3 (DomainError → handler) est nécessairement globale (le registre d'erreurs est un singleton unique pour toute l'app, pas un concept par module) ; elle remonte **30 classes DomainError sur 40 sans handler enregistré**, dans `communication/messaging`, `report-states`, et la plupart des erreurs de validation partagées (`shared/domain/errors/validation/*`). C'est un chantier réel et important, mais bien plus large qu'une correction ponctuelle — signalé, non traité ici, candidat naturel pour une session dédiée.

**Vérifié** : `tsc -p tsconfig.app.json --noEmit` : 0 erreur. `eslint --fix` sur `administrative-infrastructure` : 0 erreur. `check-semantics.js` relancé sur `infrastructure` et `infrastructure-type` après correction : règles 1/2/4 toutes "OK", règle 3 inchangée (app-wide, hors périmètre).

## Expérience 019 — Extension de `check-semantics.js` : 2 règles supplémentaires (5 et 6), généralisation confirmée hors module

**Directive reçue** : "pousse encore plus loin la validation sémantique."

**2 règles ajoutées, chacune généralisant une méthode manuelle déjà appliquée plusieurs fois cette session, pas inventée a priori** :
5. **Artefacts orphelins** — pour `application/commands/{ENTITY}/`, `application/dto/{ENTITY}/` et `domain/entities/{ENTITY}/`, chaque classe exportée doit être référencée au moins une fois ailleurs que dans son propre fichier (grep app-wide par nom de classe, mot entier). Généralise la détection manuelle de code mort déjà faite 3 fois cette session (`responsibilities-select` ×2, stubs enable/disable d'`infrastructure` — Expérience 012/016).
6. **Cérémonie VO/Entity** — pour chaque `domain/entities/{ENTITY}/{ENTITY}-{create,update,delete,filter}.entity.ts` encore présent, heuristique textuelle (présence d'un `if`/ternaire/`??`/`||`) pour détecter un pur passthrough sans transformation réelle. Généralise la règle de l'Expérience 007/010. Explicitement documentée comme heuristique — signale des candidats à vérifier à la main, jamais un verdict à exécuter aveuglément (même prudence que `domain_validators_assertion_pattern` dans `crud-entity.pattern.json`, jamais promu sans vérification humaine).

**Règle 4 étendue** au passage : vérifie désormais `{ENTITY}-update.validator.ts` en plus de `{ENTITY}-create.validator.ts` (avec `uniqId` toléré en plus sur update, jamais un champ de formulaire).

**Test de généralisation** (au-delà d'`administrative-infrastructure`, sur `departments` dans `administrative-boundary`, jamais audité avec cet outil) : le résultat confirme que les règles ne sont pas surajustées à un seul module — elles retrouvent automatiquement des défauts déjà connus mais non corrigés ailleurs (defer() manquant sur create/update, clés i18n absentes de `fr.json` — les deux explicitement documentés comme "hors périmètre" en Expérience 008/012), ET une trouvaille nouvelle, jamais signalée jusqu'ici : `departments-delete.entity.ts` et `departments-filter.entity.ts` existent encore et semblent être un pur passthrough (règle 6) — alors que `departments-create.entity.ts`/`-update.entity.ts` avaient déjà été supprimées plus tôt cette session pour `administrative-boundary`. Aucune correction faite sur `departments` — hors périmètre de cette tâche (construire l'outil), signalé pour suite.

**Vérifié** : script relancé sans erreur d'exécution sur `infrastructure`, `infrastructure-type` (6/6 règles OK) et `departments` (résultats cohérents avec les défauts déjà connus, plus une trouvaille nouvelle).

## Expérience 020 — Traitement complet de la trouvaille règle 3 : 30 handlers ajoutés, 6 classes mortes supprimées, faux positif de l'outil corrigé

**Contexte** : la règle 3 remontait 30/40 classes `DomainError` (app entière) sans handler dans `UiFeedbackService.registerDefaultHandlers()`. Signalé comme "hors périmètre, candidat pour une session dédiée" en Expérience 018. L'architecte a ensuite demandé de traiter cette trouvaille.

**Méthode** : pour chacune des 30 classes, vérification individuelle par grep du nombre réel de sites `new XError(` (hors fichier de définition) avant toute action — pas de traitement en bloc sans vérifier lesquelles sont réellement levées.

**Résultat** : 24 classes ont un vrai site d'appel unique, 2 noms recouvrent en réalité 6 classes DISTINCTES portant le même nom (`ContractRequiredError` défini 5 fois dans `report-states` — close/reject/approve/evaluate/download — et `StatusTypeRequiredError` défini 2 fois — reject/download —, `TypeRequiredError` défini 2 fois — partagé + `messaging` —), et **6 classes se sont révélées mortes** (zéro référence, y compris via chemin d'import, en dehors de leur propre fichier) : `InvalidCredentialsError`, `SearchRequiredError`, `DepartmentRequiredError` (messaging), `MunicipalityRequiredError` (messaging), et les `ContractRequiredError`/`StatusTypeRequiredError` propres à `download` dans `report-states` — jamais importées nulle part, contrairement aux 4 autres `ContractRequiredError` (close/reject/approve/evaluate), bien vivantes.

**Exécuté** :
- 30 handlers ajoutés dans `UiFeedbackService.registerDefaultHandlers()`, avec import aliasé quand un nom de classe est partagé par plusieurs fichiers distincts (ex: `import { ContractRequiredError as CloseContractRequiredError } from '.../close-contract.error'`) — nécessaire car `ErrorHandlerRegistry` indexe par référence de classe réelle (`error.constructor`), pas par nom : deux classes portant le même nom mais définies dans des fichiers différents sont deux entrées de Map distinctes.
- 6 fichiers de classes d'erreur mortes supprimés (confirmés par grep complet, pas seulement `new X(`).
- Nettoyage au passage : 3 `console.log` de debug oubliés dans `ErrorHandlerRegistry.getHandler()` (`console.log('error', error)` etc.), sans rapport direct avec la trouvaille mais trouvés en cours de lecture.

**Bug trouvé dans mon propre outil, corrigé** : après ces corrections, `check-semantics.js` (règle 3) affichait encore "2/40 : ContractRequiredError, StatusTypeRequiredError" — un FAUX POSITIF de l'outil, pas un vrai gap. Cause : la règle 3 comparait par NOM de classe seul (`Set<string>`), donc ne distinguait pas les 4 `ContractRequiredError` désormais enregistrées sous des alias (`CloseContractRequiredError`, etc.) du nom d'origine "ContractRequiredError" qu'elle cherchait. Corrigé : la règle 3 identifie maintenant chaque classe par la paire (fichier réel, nom exporté), et résout les alias d'import de `ui-feedback.service.ts` via les chemins `tsconfig.app.json` (`@shared/*`, `@presentation/*`, `@pages/*`) pour retrouver le fichier source réel de chaque `registry.register(...)`. Après correction de l'outil : 0/36 (36, pas 40, car les 6 classes mortes supprimées ne sont plus comptées).

**Vérifié** : `tsc -p tsconfig.app.json --noEmit` sur l'application entière : 0 erreur (confirme qu'aucun autre fichier ne dépendait des 6 classes supprimées, au-delà du grep). `eslint --fix` sur les 2 fichiers touchés : 0 erreur. `check-semantics.js` relancé sur `infrastructure` : 6/6 règles "OK".

## Règle d'usage

Toute nouvelle hypothèse introduite dans une discussion future doit recevoir un ID avant d'être utilisée dans un raisonnement en aval. Toute Convergence Review doit commencer par revérifier le statut des hypothèses dont dépend le résultat testé ce jour-là.

## Expérience 021 — Ouverture de l'Étape 3 : "IA en périphérie" (Charter §8), premier cas réel sur le générateur de référence

**Directive reçue** : passer à l'étape suivante après l'Étape 2 (validation). Deux roadmaps distinctes coexistaient dans les documents du projet (Charter §8 : générateur déterministe → validation → IA en périphérie ; besoin-reformule-SEOS.md §4 : ajouter un deuxième frontend de langage). Clarifié : "IA en périphérie". Sous-clarifié : appliquer l'IA au brouillon de copie i18n (erreurs de formulaire, texte SweetAlert) pour une nouvelle entité générée par `generate-reference-module.js` — jamais injecté sans relecture humaine.

**Principe retenu, conforme à la définition du Charter/besoin-reformule** : l'IA n'intervient JAMAIS sur le noyau déterministe (quels fichiers, quelles classes, quels champs sont obligatoires — tout cela reste dérivé mécaniquement, zéro IA, comme le reste de SEOS). Elle intervient UNIQUEMENT sur la valeur textuelle finale des clés i18n déjà déterminées par la structure. Séparation stricte : le générateur fixe la clé (`SEOS_REFERENCE.RESOURCES.FORM.ERROR.CREATE.NAME_REQUIRE`), l'IA ne fixe que la chaîne de caractères qui lui correspond dans `fr.json`.

**Travail déterministe réalisé d'abord (prérequis, pas la partie IA)** : le générateur `generate-reference-module.js` ne produisait pas encore de Contract/ValidateContract réel pour `resources` (VO en pur passthrough, hérité d'une version du pattern antérieure à l'Expérience 011). Mis à jour pour générer, pour `create`/`update` : `{ENTITY}-{op}.contract.ts` (champs optionnels), `{ENTITY}-{op}.validate-contract.ts` (champs stricts), `{ENTITY}-{op}.validator.ts` (assertion function, un throw par champ requis), VO appelant le validateur — même structure qu'`infrastructure`/`infrastructure-type`. `defer()` ajouté sur `create()`/`update()` du use-case (même correction qu'Expérience 008/012, désormais intégrée dès la génération plutôt que corrigée après coup). Repository (abstrait + impl) et data-mappers retypés sur `ValidateContract` plutôt que `Dto` (même correctif qu'Expérience 008 sur les casts non sûrs).

**Bug préexistant trouvé et corrigé en cours de route, sans rapport avec l'IA** : `generate-reference-module.js` utilisait `require()`/`module.exports` (CommonJS) alors que `package.json` déclare `"type": "module"` — le script plantait à l'exécution (`ReferenceError: require is not defined in ES module scope`). Jamais détecté jusqu'ici car le module `seos-reference` n'avait pas été régénéré depuis sa création initiale cette session. Corrigé en `import`/`export` ESM, même style que `check-pattern.js`/`check-semantics.js`.

**Partie IA, exécutée en dernier, sur un besoin réel identifié par l'outil** : après régénération, `check-semantics.js` (règle 2) a signalé 6 clés i18n manquantes — exactement les clés dont la structure (nom, opération) venait d'être fixée déterministiquement, sans aucun texte encore associé. J'ai rédigé le texte français pour ces 6 clés (`Le code est requis`, `Le nom est requis`, `La description est requise`, répétées sous CREATE/UPDATE) — accord grammatical correct par champ (masculin/féminin), ajouté à `fr.json` sous `SEOS_REFERENCE.RESOURCES.*`.

**Vérifié** : `tsc -p tsconfig.app.json --noEmit` sur l'application entière : 0 erreur, avant ET après l'ajout des clés i18n. `eslint --fix` sur `seos-reference` : 0 erreur. `check-pattern.js` : 83/83 (100 %). `check-semantics.js` : 6/6 règles "OK" seulement après l'ajout des clés IA — la règle 2 (i18n) était la SEULE à échouer juste avant, exactement au point de jonction entre le noyau déterministe et l'enrichissement IA, comme prévu par la conception.

**Limite explicite** : `resources` reste une entité fictive à 3 champs (code/name/description), un cas volontairement simple pour prouver le mécanisme de bout en bout. Aucune tâche de fond (#29/#30 — étendre Contract/ValidateContract aux modules réels) n'a été traitée ici ; ce chantier reste distinct et toujours en attente.

## Expérience 022 — Reprise du socle Angular réel : Contract/ValidateContract sur legal-notice, terms-use, privacy-policy (content-management)

**Directive reçue** : "Finir le socle Angular" — reprendre les tâches #29/#30 en attente depuis avant l'Étape 2/3 (Contract/ValidateContract sur les modules pas encore traités : content-management, processing, settings-security, team-organization). Premier lot traité : les 3 entités de content-management structurellement identiques (`version`+`content`, tous deux `Validators.required`) : `legal-notice`, `terms-use`, `privacy-policy`.

**État de départ, confirmé par lecture directe (pas par hypothèse)** : les 3 entités étaient encore dans l'état pré-Expérience-007/010/011 : VO en classe avec `fromDto()` statique, Entity en classe avec `fromVo()` statique (pur passthrough, aucune transformation), use-case chaînant VO→Entity→repository sur les 6 opérations (execute/create/update/publish/unpublish/delete), aucun `defer()` nulle part (même risque latent de throw synchrone non remonté que corrigé sur `infrastructure` en Expérience 008/012, non encore déclenché ici car ces VO ne validaient encore rien).

**Travail exécuté, identique aux 3 entités (mêmes noms de champs, seul le nom d'entité change), sur create/update uniquement (le périmètre explicite de la tâche)** :
- `domain/contracts/{ENTITY}/{ENTITY}-{create,update}.contract.ts` (champs optionnels) et `.validate-contract.ts` (champs stricts) créés.
- `domain/validators/{ENTITY}/{ENTITY}-{create,update}.validator.ts` créés (assertion function, un `GenericRequiredError` par champ manquant, même style que `infrastructure-create.validator.ts`).
- VO collapsés de classe à fonction (`{entity}CreateVo`/`{entity}UpdateVo`), appelant le validateur puis retournant le ValidateContract.
- `{ENTITY}-create.entity.ts` et `{ENTITY}-update.entity.ts` supprimés (confirmés 100 % passthrough par lecture directe, zéro référence restante après grep complet app-wide, pas seulement local au module).
- `domain/repositories/{ENTITY}/{ENTITY}-repository.ts` (abstrait) et son impl retypés : `create`/`update` prennent désormais un `ValidateContract`, plus une Entity.
- `infrastructure/data/mappers/{ENTITY}/{ENTITY}-{create,update}.mapper.ts` retypés pareillement (Entity → ValidateContract).
- `application/use-cases/{ENTITY}/{ENTITY}.use-case.ts` : `create()`/`update()` réécrits pour appeler directement la fonction VO et envelopper l'appel repository dans `defer()` (correctif Expérience 008/012 appliqué dès l'écriture, pas après coup).
- Clés i18n ajoutées à `fr.json` sous `CONTENT_MANAGEMENT.{ENTITY}.FORM.ERROR.{CREATE,UPDATE}.{VERSION,CONTENT,UNIQ_ID}_REQUIRE`, texte français rédigé directement (cas trivial, pas de démonstration IA nécessaire ici — la démonstration IA-en-périphérie a déjà été faite en Expérience 021 sur un cas dédié).
- Aucun nouveau handler `UiFeedbackService` requis : `GenericRequiredError` est déjà enregistré une seule fois de façon générique (vérifié par lecture directe du fichier), pas par sous-classe.
- La couche Facade/Command/Handler/commands-mapper au-dessus du use-case n'a pas été touchée : elle passe déjà un objet structurellement compatible (Dto à champs requis, sous-ensemble valide du Contract à champs optionnels) — même principe que pour `infrastructure`.

**Bug de généralisation trouvé dans mon propre outil `check-semantics.js`, corrigé** : la règle 4 (Validators.required vs validateur de domaine) cherchait le form-store uniquement sous `presentation/store/{ENTITY}/{ENTITY}-form.store.ts`. Or `content-management` utilise encore l'ancienne convention `application/store/{ENTITY}/{ENTITY}-form.store.ts` (non migrée), documentée par ailleurs dans ce registre. Résultat avant correction : la règle 4 aurait rapporté "fichier absent" sur les 3 entités, un faux négatif de couverture (pas un faux positif de bug), qui aurait rendu le check inutile sur tout module non migré. Corrigé : la règle 4 essaie désormais les deux chemins connus, dans l'ordre `presentation/store` puis `application/store`, et utilise le premier trouvé. Revérifié après correction : `infrastructure` (convention `presentation/store`) toujours 0 souci sur cette règle — la correction n'introduit pas de régression sur le cas déjà validé.

**Trouvaille annexe, documentée mais volontairement PAS traitée dans ce lot (hors périmètre explicite "Contract/ValidateContract")** : la règle 6 (heuristique cérémonie VO/Entity) signale, pour les 3 entités, que `{ENTITY}-delete.entity.ts` et `{ENTITY}-filter.entity.ts` restent des classes sans transformation détectée (candidats passthrough, même règle qu'Expérience 007/010 mais appliquée à delete/filter plutôt qu'à create/update). Vérification manuelle complémentaire sur `legal-notice-delete.entity.ts` : il porte en réalité 2 méthodes supplémentaires (`appliesToAdminScope()`, `describe()`) — jamais appelées nulle part dans l'app (confirmé par grep), donc du code mort de cérémonie, pas une vraie transformation. Le même duo de méthodes existe à l'identique sur les entités `publish`/`unpublish` des 3 entités (probable copie d'un template ancien). Ce nettoyage (collapse delete/filter, suppression du code mort `appliesToAdminScope`/`describe`) est laissé en tâche de suivi distincte, pour ne pas mélanger deux décisions d'architecture différentes dans un même lot.

**Vérifié, sur les 3 entités et sur l'app entière** : `tsc -p tsconfig.app.json --noEmit` : 0 erreur (app entière, pas juste content-management). `eslint --fix` sur les fichiers touchés des 3 entités : 0 erreur, seulement des warnings préexistants sans rapport (lignes de fichier, retours de fonction non typés sur du code non touché). `check-semantics.js` relancé sur `legal-notice`, `terms-use`, `privacy-policy` : règles 1 à 5 systématiquement "OK" sur les 3 ; règle 6 signale le même finding delete/filter sur les 3 (cohérent avec leur clonage structurel confirmé).

**Tâche #34 marquée complétée. Reste #35 (`home`, `slide`, `news` — formulaires dynamiques avec `Validators.required` conditionnels) et #30 (`processing`, `settings-security`, `team-organization` — pas encore scopés en détail).**

## Expérience 023 — Nettoyage du code mort delete/filter/publish/unpublish (legal-notice, terms-use, privacy-policy)

**Directive reçue** : "travaille sur le reste code mort" — référence directe à la trouvaille annexe documentée en Expérience 022 (candidats passthrough `{ENTITY}-delete.entity.ts`/`{ENTITY}-filter.entity.ts` signalés par la règle 6 de `check-semantics.js`, plus les méthodes `appliesToAdminScope()`/`describe()` jamais appelées, confirmées mortes par grep).

**Vérification complémentaire avant exécution** : lecture directe des 4 opérations restantes (delete/filter/publish/unpublish) sur les 3 entités. Confirmé, de façon strictement symétrique sur les 3 :
- `{ENTITY}-delete.entity.ts`, `{ENTITY}-publish.entity.ts`, `{ENTITY}-unpublish.entity.ts` : chacun porte les deux mêmes méthodes mortes `appliesToAdminScope()`/`describe()` (probable copie d'un template ancien), zéro appel dans toute l'app (grep complet). Une fois ces méthodes retirées, il ne reste plus qu'un wrapper `{ uniqId }` sans aucune transformation — cérémonie pure au sens Expérience 007/010.
- `{ENTITY}-filter.entity.ts` : passthrough pur (aucune logique), confirmé sur les 3.
- `{ENTITY}-filter.vo.ts` : porte la seule vraie transformation de tout le groupe (`search?.trim() || undefined`), identique sur les 3.
- `{ENTITY}-delete/publish/unpublish.vo.ts` : passthrough pur (constructeur qui recopie `uniqId`, rien d'autre), identique sur les 3.

**Traitement appliqué, identique aux 3 entités** :
- `{entity}DeleteVo`/`{entity}PublishVo`/`{entity}UnpublishVo` réduits à une fonction identité (`(dto) => dto`), même convention que `infrastructureDeleteVo` en administrative-infrastructure — le VO est conservé comme point d'extension futur (validation), mais ne fait plus semblant de transformer quoi que ce soit via une classe.
- `{entity}FilterVo` collapsé de classe à fonction, la transformation réelle (trim) préservée à l'identique ; le type de retour devient une interface exportée du même nom (`{Entity}FilterVo`) puisque le Repository abstrait a besoin d'un type pour son paramètre `readAll` et qu'aucun Contract/ValidateContract n'existe pour `filter` dans ce module (voir limite ci-dessous).
- Les 8 fichiers `{ENTITY}-{delete,filter,publish,unpublish}.entity.ts` supprimés par entité (24 fichiers au total sur les 3 entités), donc les méthodes mortes `appliesToAdminScope`/`describe` disparaissent avec eux plutôt que d'être éditées à la marge.
- `domain/repositories/{ENTITY}/{ENTITY}-repository.ts` (abstrait) et son impl retypés : `delete`/`publish`/`unpublish` prennent directement le Dto correspondant, `readAll` prend `{Entity}FilterVo`.
- `infrastructure/data/mappers/{ENTITY}/{ENTITY}-{delete,publish,unpublish,filter}.mapper.ts` retypés pareillement (Dto/Vo directement, plus d'Entity).
- `application/use-cases/{ENTITY}/{ENTITY}.use-case.ts` : les 4 méthodes simplifiées pour appeler la fonction VO directement sans étape Entity intermédiaire.

**Limite explicite, non traitée ici, volontairement** : `{ENTITY}FilterDto` porte des champs `startDate`/`endDate` optionnels sans aucune validation de cohérence (pas de vérification `startDate <= endDate`), alors que la clarification déjà enregistrée dans `design_decisions_v9` indique qu'un `filter.contract`/`validator` est justifié précisément quand le filtre a des champs date. Construire ce Contract/ValidateContract complet (avec une erreur de type `InvalidDateRangeError`, déjà existante et déjà câblée dans `UiFeedbackService` pour d'autres modules) est une tâche distincte, non demandée ici ("code mort" ≠ "fonctionnalité de validation manquante") — laissée en tâche de suivi si le besoin se confirme.

**Vérifié** : `tsc -p tsconfig.app.json --noEmit` sur l'application entière : 0 erreur, avant et après suppression des 24 fichiers Entity. `eslint --fix` sur les fichiers touchés des 3 entités : 0 erreur (seulement des warnings préexistants sans rapport). `check-semantics.js` relancé sur les 3 entités : 6/6 règles "OK" — la règle 6, qui signalait encore 2 findings par entité avant ce nettoyage, ne signale plus rien.

**Tâche #36 marquée complétée.**

## Expérience 024 — Contract/ValidateContract sur home, slide, news (content-management) : le lot le plus hétérogène du module

**Directive reçue** : "Reste dans #29 : home, slide, news / Analyse complete de l'implementation, avant de nettoyer." Analyse complète livrée d'abord (voir échange), révélant un état bien plus disparate que legal-notice/terms-use/privacy-policy : chaque entité était à un stade différent de maturité. Décisions tranchées ensuite par l'architecte : (1) convention `GenericRequiredError`+clé i18n partout, y compris sur `slide` qui avait déjà un mécanisme fonctionnel à base de classes dédiées ; (2) corriger l'asymétrie create/update en construisant les deux symétriquement ; (3) ajouter `defer()` partout où absent.

**État de départ constaté par lecture directe** :
- `home` : cycle enable/disable, VO/Entity en classes à props privées + getters (convention encore plus ancienne que legal-notice), ZÉRO validateur alors que le formulaire impose 7 champs requis, mais une vraie logique déjà active via `DatePeriod.create(startDate, endDate)` (VO partagé, lève `InvalidStartDateError`/`InvalidEndDateError`/`InvalidDateRangeError`, déjà câblés). Aucun `defer()` : le throw synchrone de `DatePeriod.create()` dans `fromDto()` n'était jamais intercepté par le pipeline d'erreur — bug Expérience 008/012 jamais corrigé ici.
- `slide` : le plus mûr des trois. `defer()` déjà présent sur les 5 opérations. Un validateur pré-Expérience-011 existait déjà (`application/validators/slide/slide-create.validator.ts`), fonctionnel et entièrement câblé, mais **sans équivalent update** : la mise à jour d'un slide ne passait que par `DatePeriod.create()`, aucun champ obligatoire de la création (title, type, image/video selon type, platforms) n'était revérifié.
- `news` : le plus nu. VO/Entity purs passthrough, aucun validateur nulle part, aucun `defer()`. Logique de requis conditionnel image/video selon `type` dupliquée quasi à l'identique de `slide` (même paire de fonctions `updateValidatorsByType`/`typeMediaConsistencyValidator`).

**Travail exécuté, identique en structure sur les 3 entités (create + update)** :
- Contract/ValidateContract créés avec `GenericRequiredError`+clé i18n pour tous les champs réellement requis par le formulaire.
- Champs requis conditionnellement (image si `type===IMAGE`, video si `type===VIDEO`, sur `slide` et `news`) : logique conditionnelle directement dans la fonction de validation — premier cas de ce genre dans le schéma SEOS (jusqu'ici tous les validateurs Contract étaient des ensembles de champs requis statiques, sans discriminant).
- Cohérence croisée boutons (`buttonLabel`/`buttonUrl`, sur `home` et `slide`) : si l'un est renseigné, l'autre devient requis — même mécanique que la règle deja utilisee pour rapporter une erreur ponctuelle (`GenericRequiredError`), pas une nouvelle classe dédiée.
- `home`/`slide` : la fonction VO calcule aussi `DatePeriod.create(contract.startDate, contract.endDate)` après validation des champs requis — réutilisation telle quelle du VO partagé existant, pas de duplication de la logique de cohérence de dates.
- VO classes → fonctions, Entity create/update passthrough supprimées (6 fichiers), repository/mapper/use-case retypés sur les interfaces `{Entity}CreateProps`/`{Entity}UpdateProps` déjà existantes (au lieu d'une nouvelle interface ValidateContract dédiée, pour `home`/`slide` dont le VO produit un objet enrichi par `DatePeriod` — le ValidateContract sert uniquement à faire passer l'assertion de type, la forme finale réellement transmise au repository reste `{Entity}{Op}Props`).
- `defer()` ajouté sur `home.use-case.ts` et `news.use-case.ts` (`slide` l'avait déjà).
- Ancien fichier `application/validators/slide/slide-create.validator.ts` supprimé après confirmation (grep complet) qu'aucune référence externe ne subsistait.

**Nettoyage en cascade, confirmé avant suppression** : les 8 classes d'erreur dédiées que l'ancien validateur de `slide` utilisait (`TitleRequiredError`, `ImageRequiredError`, `VideoRequiredError`, `TimeDurationRequiredError`, `TimeDurationInvalidError`, `PlatformsRequiredError`, `StartDateRequiredError`, `EndDateRequiredError`) n'avaient AUCUNE autre référence dans l'app (vérifié classe par classe, grep complet hors fichier de définition et hors `ui-feedback.service.ts`) — supprimées avec leurs handlers `registry.register(...)` correspondants. `TypeRequiredError` et `DateRangeInvalidError`, en apparence similaires, ont été conservées : la première est partagée par `report-states`/`communication`, la seconde est le mécanisme standard des validateurs de FILTRE dans plusieurs modules (`administrative-infrastructure`, `report-states`, `communication`, `seos-reference`) — confirmant au passage qu'un filtre `startDate`/`endDate` avec validation de cohérence est un pattern déjà établi ailleurs, simplement pas encore présent sur `legal-notice`/`terms-use`/`privacy-policy`/`home`/`slide`/`news` (cf. limite déjà notée en Expérience 023).

**Trois bugs réels trouvés par `check-semantics.js` dans son propre code, corrigés** :
1. Règle 4 ne reconnaissait pas l'idiome `!contract.field?.length` (champs tableau, ex. `platforms: string[]`) — faux positif sur `home`. Regex étendue pour reconnaître ce motif.
2. Règle 4 ne reconnaissait que des conditions `if (contract.field === undefined|null)` isolées, pas composées — faux positif sur `timeDuration` de `slide` (`if (contract.timeDuration === null || contract.timeDuration === undefined)`). Regex assouplie pour matcher n'importe où dans la parenthèse.
3. Règle 4 ne retirait pas les commentaires avant de tester la présence de `Validators.required` dans un bloc de FormControl — un `// Validators.required,` commenté sur `subtitle`/`content` de `slide` était compté comme actif. Corrigé en supprimant les commentaires ligne avant le test.
Les trois corrections ont été revérifiées sans régression sur `infrastructure`, `legal-notice`, `home` (toujours "OK" sur la règle 4 après coup).

**Limite explicite, non corrigée (trop coûteuse pour le bénéfice, documentée plutôt que traitée)** : la règle 4 continue de signaler `image` comme "requis au formulaire mais non vérifié par le domaine" sur `slide` et `news`, alors que le domaine le vérifie bien — mais de façon CONDITIONNELLE (`if (contract.type === 'IMAGE' && !contract.image)`), un motif que la règle 4, volontairement simple (regex, pas d'AST), ne peut pas distinguer d'une absence de vérification. Confirmé manuellement que le comportement réel est correct (identique à l'ancien validateur de `slide`). Accepté comme limite connue du heuristique plutôt que généralisé davantage.

**Trouvailles annexes documentées, non traitées (hors périmètre Contract/ValidateContract)** :
- `slide-create.mapper.ts`/`slide-update.mapper.ts` ne mappent jamais `buttonUrl` vers l'API (seul `buttonLabel` l'est) — bug préexistant, non introduit ici, laissé tel quel (comportement inchangé).
- Rule 6 signale à nouveau `{ENTITY}-delete.entity.ts`/`{ENTITY}-filter.entity.ts` comme cérémonie passthrough sur les 3 entités — même trouvaille que sur legal-notice/terms-use/privacy-policy (Expérience 022), laissée en l'état pour ce lot, non redemandée explicitement cette fois.
- `news-form.store.ts` : `hashtagsErrors` (computed lisant `minHashtags`/`maxHashtags`/`duplicateHashtags` sur le contrôle `hashtags`) ne peut jamais renvoyer d'erreur — aucun validateur n'est jamais posé sur ce `FormControl`. Fonctionnalité UI probablement commencée puis abandonnée, hors périmètre backend.

**Vérifié, sur les 3 entités et sur l'app entière** : `tsc -p tsconfig.app.json --noEmit` : 0 erreur (app entière, à chaque étape intermédiaire). `eslint --fix` sur les fichiers touchés : 0 erreur (seulement des warnings préexistants, plus deux nouveaux warnings de complexité cyclomatique sur `slide-create/update.validator.ts` — 19-20 contre un seuil de 15 — dus au nombre de champs, acceptés tels quels, pas d'erreur bloquante). `check-semantics.js` : règles 1, 2, 3, 5 systématiquement "OK" sur les 3 ; règle 4 "OK" complet sur `home`, limité au seul `image` conditionnel connu sur `slide`/`news` ; règle 6 signale le même finding delete/filter que le lot précédent.

**Tâches #35 et #29 marquées complétées : les 6 entités de content-management (legal-notice, terms-use, privacy-policy, home, slide, news) ont désormais toutes un Contract/ValidateContract réel, classless, avec `defer()`. Reste #30 (processing, settings-security, team-organization), non encore scopé en détail.**

## Expérience 025 — Contract/ValidateContract sur processing, settings-security, team-organization (5 entités, 3 modules)

**Directive reçue** : "vas'y" — continuation directe de la tâche #30, seule tâche restante de l'épopée "Finir le socle Angular". Aucune nouvelle question posée : les trois décisions déjà tranchées par l'architecte pour le lot Expérience 024 (convention `GenericRequiredError`+i18n uniforme, Contract/ValidateContract construits pour create ET update dans la même passe, `defer()` ajouté partout où absent) ont été appliquées telles quelles, ces modules ne présentant aucun fork de convention nouveau justifiant une nouvelle question.

**Scoping préalable (analyse avant code, même discipline qu'Expérience 024)** : lecture systématique des `use-cases/**/*.use-case.ts` des 3 modules pour distinguer les entités CRUD réelles (avec `create()`/`update()`) des sous-modules purement lecture (queues/tâches en lecture seule dans `processing`, `access-logs` dans `settings-security`, `daily-goal`/`agents-performances` dans `team-organization`). Résultat : exactement 5 entités concernées — `tasks-actions` (processing), `users` et `profiles-permissions` (settings-security), `participants` et `teams` (team-organization).

**État de départ constaté par lecture directe, hétérogène comme chaque lot précédent** :
- `tasks-actions` : seule entité du lot à avoir déjà une validation active, mais via `throw new Error('...')` brut — ni `GenericRequiredError`, ni classe `DomainError`, ni clé i18n, ni enregistrement dans `UiFeedbackService`. Un champ `isConform` (enum `Conformity`) explicitement vérifié non-null. Particularité unique dans tout le schéma SEOS : `reportUniqId` est un champ requis par le DTO mais jamais présent dans le formulaire Angular (`ActionsTreatmentFormStore`) — il est injecté par l'appelant au moment du submit, donc absent de `formAllFields` et non comparable par la règle 4 de `check-semantics.js` (confirmé : la règle l'ignore correctement puisqu'elle ne compare que les champs présents dans le formulaire). Mapper `tasksActionsCreateMapper`/`tasksActionsUpdateMapper` dépend en plus d'un `ConformityMapper` injecté séparément (convention à part, différente de tout ce qui a été vu jusqu'ici).
- `users` : VO totalement passthrough (zéro validation). Champ `role` mort et commenté sur toute la chaîne (Contract exclu, VO, Entity, mapper, formulaire, store) — feature manifestement désactivée intentionnellement, non touchée (ni supprimée ni réactivée), préservée à l'identique en commentaire dans les nouveaux fichiers.
- `profiles-permissions` : VO passthrough. Champ `permissions: Record<string, string[]>` (arbre de permissions, pas un tableau) — premier cas du schéma SEOS où le typage du champ "requis" n'est ni `string` ni `string[]` mais un objet ; vérifié par simple présence (`!contract.permissions`), pas par profondeur (nombre de clés), cohérent avec le niveau de rigueur des autres champs "requis" du même lot.
- `participants` : VO fait une vraie transformation (`normalizePhoneNumber`), préservée telle quelle dans la fonction VO. Champ `role` devient conditionnellement requis quand `team` est renseigné (`ParticipantsFormStore` bascule `Validators.required` sur `role` via un effect à chaque changement de `team`) — troisième occurrence du motif "requis conditionnel" après `image`/`video` selon `type` (Expérience 024), cette fois piloté par une relation entre deux champs plutôt que par un discriminant enum.
- `teams` : VO passthrough, champ `code` mort et commenté (jamais réactivé, préservé en commentaire). Bug de typage réel trouvé et corrigé au passage : `permissions` était `string[]` sur le Contract de création mais `any[]` sur celui de mise à jour — unifié en `string[]` dans les deux `ValidateContract`, cohérent avec le mapper qui fait `permissions.map(Number)` des deux côtés.

**Travail exécuté, identique en structure sur les 5 entités (create + update)** : Contract (champs optionnels) → validateur (fonction d'assertion, `GenericRequiredError`+clé i18n par champ requis, calqué sur les champs réellement marqués `Validators.required` dans le formulaire correspondant) → ValidateContract (champs stricts) → fonction VO (appelle le validateur puis renvoie le ValidateContract, transformations réelles préservées à l'identique — normalisation téléphone sur `participants`, défaut `shouldNotifyUser ?? false` sur `tasks-actions`). Entities create/update supprimées (10 fichiers), repository abstrait + impl + mapper(s) retypés sur ValidateContract, `use-case.ts` : `create()`/`update()` enveloppés dans `defer()`.

**Bug préexistant découvert et corrigé, hors périmètre Contract mais bloquant pour une compilation propre** : `InfrastructureTypeFilterDto` (administrative-infrastructure, déjà migré en Expérience 013) portait encore `isActive?: Status` alors que `InfrastructureTypeFilterControl`/`InfrastructureTypeFilterStore` avaient déjà été renommés en `status` dans une passe antérieure — renommage resté incomplet, 3 fichiers oubliés (`infrastructure-type-filter.dto.ts`, `infrastructure-type-filter.vo.ts`, `infrastructure-type-filter.mapper.ts`, `infrastructure-type.mapper.ts` côté query). Corrigé par cohérence avec la majorité déjà migrée (`status` partout). Note : l'utilisateur a ensuite continué à faire évoluer ce même fichier en parallèle (ajout de `startDate`/`endDate`, puis son propre Contract/ValidateContract) — changements observés mais non touchés, hors périmètre de cette tâche.

**Limite d'outillage découverte, non corrigée** : `check-semantics.js` suppose une convention à plat `{moduleRoot}/application/use-cases/{entityName}/{entityName}.use-case.ts` (dossier == préfixe de fichier). `processing` regroupe plusieurs entités sous un même dossier générique (`tasks/` contient `tasks`, `tasks-actions`, `tasks-actions-type`) — la convention ne tient pas, l'outil ne peut pas localiser les fichiers pour `tasks-actions`. Les 6 règles ont donc été vérifiées manuellement pour cette entité (defer présent, clés i18n présentes, `GenericRequiredError` déjà enregistré app-wide, champs requis du formulaire vérifiés un par un contre le validateur, aucune référence orpheline aux Entity supprimées). Par ailleurs, l'exécution de l'outil sur `teams` (team-organization) a timeout de façon reproductible (>20s, tuée manuellement) — cause précise non déterminée (le dossier `domain/entities/teams/` compte 16 fichiers, comparable à `profiles-permissions` qui n'a pas ce problème), traité comme une limite de performance de l'outil sur ce module plutôt qu'investigué davantage : `tsc` et `eslint` app-wide couvrent déjà la correction réelle, et la vérification manuelle (grep zéro référence aux Entity supprimées) confirme l'absence de régression.

**Vérifié, sur les 5 entités et sur l'app entière, à chaque étape intermédiaire** : `tsc -p tsconfig.app.json --noEmit` : 0 erreur imputable à ce lot (les seules erreurs résiduelles, sur `infrastructure-type-filter.vo.ts`, proviennent d'une édition parallèle de l'utilisateur sur ce même fichier, non liée à cette tâche). `eslint --fix` sur tous les fichiers touchés des 5 entités : 0 erreur. `check-semantics.js` : `users`, `profiles-permissions`, `participants` : règles 1/2/3/5 systématiquement "OK", règle 4 "OK" complet (aucun écart requis formulaire ↔ domaine), règle 6 signale le même finding delete/filter déjà connu et volontairement non traité pour ce lot (même périmètre que le lot précédent) ; `tasks-actions` vérifié manuellement (limite d'outillage ci-dessus) ; `teams` non vérifiable par l'outil (timeout), couvert par `tsc`/`eslint`/grep manuel.

**Tâche #30 marquée complétée : les 8 modules avec entités CRUD (administrative-boundary, administrative-infrastructure, content-management, processing, settings-security, team-organization) ont désormais tous un Contract/ValidateContract réel, classless, avec `defer()`, sur toutes leurs opérations create/update. Reste ouvert, non demandé explicitement : le nettoyage code-mort delete/filter (rule 6) sur home/slide/news/tasks-actions/users/profiles-permissions/participants/teams (même trouvaille répétée à chaque lot depuis Expérience 022), et la construction d'un Contract/ValidateContract pour les opérations FILTER avec dates (DateRangeInvalidError déjà disponible), toujours pas traitée nulle part.**

## Expérience 026 — Nettoyage code mort delete/filter/enable/disable/publish/unpublish, généralisé aux 8 entités restantes (content-management, processing, settings-security, team-organization)

**Directive reçue** : "vas'y pour le netoyage du code mort" — clôture explicite de la trouvaille laissée ouverte à la fin de l'Expérience 025 (rule 6 de `check-semantics.js` signalant, à chaque lot depuis Expérience 022, la même cérémonie passthrough `{ENTITY}-{delete,filter,enable,disable,publish,unpublish}.entity.ts` + méthodes mortes `appliesToAdminScope()`/`describe()`). Même traitement que l'Expérience 023 (legal-notice/terms-use/privacy-policy), généralisé ici aux 8 entités restantes qui n'avaient pas encore reçu ce nettoyage : `home`, `slide`, `news` (content-management), `tasks-actions` (processing, delete+filter seulement — pas d'enable/disable/publish/unpublish sur cette entité non "publiable"), `users`, `profiles-permissions` (settings-security), `participants`, `teams` (team-organization).

**Vérification préalable, répétée entité par entité avant toute suppression** : lecture directe de chaque fichier `{ENTITY}-{delete,enable,disable,filter}.entity.ts` (ou `publish`/`unpublish` pour `news`), confirmant la même structure strictement passthrough sur les 8 entités (constructeur recopiant les champs + `static fromVo()` + `appliesToAdminScope(): boolean { return this.uniqId === 'ADMIN_ACTION'; }` + `describe(): string { return JSON.stringify({...}); }`). Puis, pour chaque module, `grep -rn "appliesToAdminScope\|\.describe("` filtré hors fichiers `.entity.ts` eux-mêmes, sur l'ensemble du module — zéro appel trouvé dans les 4 modules, à l'exception d'une seule ligne déjà commentée et inerte dans `access-logs.use-case.ts` (`// if (entity?.appliesToAdminScope())`), non liée à `users`, non touchée.

**Traitement appliqué, identique aux 8 entités** :
- VO de `delete`/`enable`/`disable`/`publish`/`unpublish` réduits à une fonction identité (`(dto) => dto`), même convention qu'Expérience 023/administrative-infrastructure.
- VO de `filter` collapsé de classe à interface exportée + fonction, transformation réelle (`search?.trim() || undefined`) préservée à l'identique sur toutes les entités qui en disposent (absente sur `tasks-actions`, dont le filtre ne porte que `uniqId`, scopé à un rapport donné — pas de champ texte à trim).
- Les fichiers `{ENTITY}-{delete,enable,disable,filter,publish,unpublish}.entity.ts` supprimés (28 fichiers au total sur les 8 entités : 4×3 pour home/slide/news, 2 pour tasks-actions, 4×4 pour users/profiles-permissions/participants/teams).
- `domain/repositories/{ENTITY}/{ENTITY}-repository.ts` (abstrait), son impl, et les mappers correspondants retypés pour accepter directement le Dto (ou le `{Entity}FilterVo`) au lieu de l'Entity.
- `application/use-cases/{ENTITY}/{ENTITY}.use-case.ts` simplifié : les trois étapes `const vo = XxxVo.fromDto(dto); const entity = XxxEntity.fromVo(vo); return this.repository.op(entity);` réduites à `return this.repository.op(xxxVo(dto));` — enveloppé dans `defer()` uniquement là où c'était déjà le cas avant ce nettoyage (`slide` sur les 3 opérations, héritage d'un batch Contract antérieur).
- Particularité `profiles-permissions`/`teams` : le mapper de filtre est resté une classe injectable (`ProfilesPermissionsFilterMapper`/`TeamsFilterMapper`, avec dépendance à un `StatusMapper` injecté) plutôt qu'une fonction pure, convention préexistante et orthogonale à ce nettoyage — seul le type du paramètre a changé (`Entity` → `Vo`), la forme classe/injectable n'a pas été touchée.

**Trouvaille annexe confirmée mais non traitée (hors périmètre)** : dans `profiles-permissions` et `teams`, il existe un second niveau d'entités `{ENTITY}-users-filter.entity.ts` / `{ENTITY}-participants-filter.entity.ts` (listes imbriquées d'utilisateurs/participants à l'intérieur d'un profil ou d'une équipe) qui portent un nom proche mais ne font PAS partie de la cérémonie ciblée ici — non passthrough, non signalées par la règle 6, non supprimées. Confirmé par lecture qu'elles restent hors périmètre.

**Vérifié, sur les 8 entités et sur l'app entière, à chaque étape intermédiaire** : grep app-wide pour chaque nom de classe Entity supprimée — aucune référence orpheline (les seules occurrences résiduelles des chaînes `XxxFilterEntity` pointent vers des entités sœurs non concernées : `ProfilesPermissionsUsersFilterEntity`, `TeamsParticipantsFilterEntity`, `TasksFilterEntity`/`TasksActionsTypeFilterEntity`, `HomeFindOneFilterEntity`, etc.). `tsc -p tsconfig.app.json --noEmit` sur l'application entière : 0 erreur, relancé deux fois consécutives à la fin du lot users/profiles-permissions et à la fin du lot participants/teams. `eslint --fix` sur l'ensemble des fichiers touchés (32 fichiers pour home/slide/news lors d'un lot antérieur, plus 21 fichiers pour users/profiles-permissions, plus 21 fichiers pour participants/teams dans cette session) : 0 erreur à chaque exécution.

**Limite d'outillage, déjà connue depuis Expérience 025, non recontrôlée ici** : `check-semantics.js` reste non fiable sur `processing/tasks-actions` (convention à plat non respectée) et timeout de façon reproductible sur `team-organization/teams` — la vérification de ce lot s'est donc appuyée, comme lors du lot précédent, sur la triade manuelle grep + `tsc` + `eslint` plutôt que sur l'outil automatisé pour ces deux modules spécifiquement.

**Tâches #46 à #49 marquées complétées : le nettoyage code-mort delete/filter/enable/disable/publish/unpublish est désormais fait sur les 11 entités qui en avaient besoin dans tout le projet (legal-notice, terms-use, privacy-policy — Expérience 023 ; home, slide, news, tasks-actions, users, profiles-permissions, participants, teams — cette expérience). Reste ouvert, non demandé explicitement, inchangé depuis Expérience 023/025 : la construction d'un Contract/ValidateContract pour les opérations FILTER avec dates (`DateRangeInvalidError` déjà disponible et câblée), sur les entités qui portent `startDate`/`endDate` en filtre (home, slide, news — `tasks-actions`/`users` n'ont pas ce besoin, leur filtre ne porte pas de dates).**

## Expérience 027 — Contract/ValidateContract pour le FILTER avec dates : home, slide, news (content-management)

**Directive reçue** : "vas'y, demarre alors" — feu vert direct pour traiter la dernière trouvaille laissée ouverte depuis Expérience 023, répétée sans être traitée à chaque lot suivant (024, 025, 026) : `home`, `slide`, `news` acceptent `startDate`/`endDate` en filtre sans jamais vérifier `startDate <= endDate`.

**Recherche préalable de convention existante (avant d'écrire du code)** : recherche app-wide de `DateRangeInvalidError` (l'erreur explicitement pré-enregistrée dans `UiFeedbackService`, signalée comme disponible depuis Expérience 023). Résultat inattendu : **deux conventions distinctes et concurrentes** coexistent déjà dans le code réel pour valider un intervalle de dates en filtre, pas une seule :
1. **Convention A** (`administrative-infrastructure`, `report-states`, `communication`, `seos-reference`) : `{ENTITY}FilterContract` (champs `startDate`/`endDate` typés `Date`) → `validate{Entity}Filter()` (fonction d'assertion qui compare `startDate.getTime() > endDate.getTime()` et lève `DateRangeInvalidError` directement) → VO. Aucune n'a de `ValidateContract` distinct : le filtre ne rend jamais un champ "non optionnel", donc rien à durcir après validation — seul `administrative-infrastructure` conserve en plus un `{entity}FilterEntity()` (fonction, pas classe) qui applique un défaut réel (`endDate = now()` si seul `startDate` est fourni).
2. **Convention B** (`administrative-boundary`, `team-organization` — `daily-goal`/`agents-performances`, `settings-security` — `access-logs`, `requests`/`processing`/`finalization` — `tasks`/`queues`/`all`) : champs `startDate`/`endDate` typés `string`, VO encore en **classe** (`RegionsFilterVo`, `DailyGoalFilterVo`, non concerné par le nettoyage classless car ces modules n'ont pas encore été traités), qui construit directement un objet `DatePeriod` via `DatePeriod.create(start, end)` — la même `DatePeriod` (avec ses erreurs propres `InvalidStartDateError`/`InvalidEndDateError`/`InvalidDateRangeError`) déjà utilisée par `home`/`slide` pour leurs opérations `create`/`update` (Expérience 024).

**Décision d'architecte (tranchée seule, cohérence locale retenue sur cohérence globale)** : Convention B retenue pour `home`/`slide`/`news`, pas Convention A — parce que ces trois entités dépendent déjà de `DatePeriod` pour leurs Contracts `create`/`update` dans ce même module ; introduire `DateRangeInvalidError` en plus aurait fait cohabiter deux mécanismes d'erreur différents pour le même concept (cohérence de plage de dates) à l'intérieur d'un seul module. Autrement dit : cohérence intra-module choisie plutôt que uniformité inter-module — les deux conventions existaient déjà avant cette tâche, aucune des deux n'est donc "la" convention canonique du schéma SEOS, le choix se fait au cas par cas selon ce que l'entité utilise déjà ailleurs.

**Travail exécuté, identique sur les 3 entités** :
- `domain/contracts/{entity}/{entity}-filter.contract.ts` créé : interface strictement identique en forme au `{Entity}FilterDto` existant (aucun champ ne devient non-optionnel — un filtre n'a structurellement rien à durcir après validation, cohérent avec Convention A où aucun `ValidateContract` distinct n'existe non plus pour ce motif).
- `domain/validators/{entity}/{entity}-filter.validator.ts` créé : `validate{Entity}Filter(contract): asserts contract is {Entity}FilterContract` qui appelle `DatePeriod.create(contract.startDate, contract.endDate)` uniquement si l'un des deux champs est renseigné (laisse `DatePeriod` lever ses propres erreurs déjà câblées, aucune nouvelle classe d'erreur introduite).
- `{entity}-filter.vo.ts` (déjà collapsé en fonction lors du nettoyage code-mort, Expérience 026) : ajout d'un seul appel `validate{Entity}Filter(dto ?? {})` en tête de fonction, avant la construction de l'objet retourné. Forme de retour **inchangée à dessein** (toujours `startDate`/`endDate` en `string` bruts, pas un objet `DatePeriod`) : le mapper API de ces trois entités transmet déjà les dates en chaînes brutes au filtre (contrairement à `create`/`update` qui transmettent un objet `DatePeriod` sérialisé via `ApiDateMapper`) — changer la forme du VO de filtre aurait forcé une refonte du mapper sans bénéfice, la validation seule était demandée, pas une réingénierie de la sérialisation. Décision explicitement scope-limitante, dans le même esprit que "code mort ≠ fonctionnalité manquante" (Expérience 023).
- **Bug de classe latent évité, pas hérité** : ajouter un appel qui peut lever une exception synchrone à l'intérieur de `{entity}FilterVo()` réintroduit exactement le risque déjà documenté et corrigé pour `create`/`update` (Expérience 008/012 : un throw synchrone dans un VO appelé directement par un `use-case.execute()` non enveloppé casse le pipeline d'erreur RxJS). Vérifié : aucune des trois méthodes `execute()` de `home.use-case.ts`/`slide.use-case.ts`/`news.use-case.ts` n'était enveloppée dans `defer()` avant cette tâche (elles n'avaient jamais eu besoin de l'être, aucune validation n'y throwait). Les trois `execute()` ont donc été enveloppées dans `defer()`, symétriquement à `create()`/`update()`/`delete()` qui l'étaient déjà. Note : le précédent direct trouvé dans le code existant (`close.use-case.ts`, `report-states`, Convention A) n'enveloppe PAS son `execute()` dans `defer()` alors qu'il devrait par le même raisonnement — bug latent préexistant, hors périmètre de cette tâche (module non touché), non corrigé, simplement constaté.

**Vérifié, sur les 3 entités et sur l'app entière** : `tsc -p tsconfig.app.json --noEmit` : 0 erreur, relancé après l'ajout des Contracts/validateurs et de nouveau après l'ajout des `defer()`. `eslint --fix` sur les 12 fichiers touchés (3 Contracts, 3 validateurs, 3 VO, 3 use-cases) : 0 erreur. `check-semantics.js` relancé sur les 3 entités : règles 1/2/3/5/6 "OK" partout (règle 1 — `defer()` — désormais vérifiée vraie sur les 3, alors qu'elle ne testait pas `execute()` avant) ; règle 4 signale encore le même faux positif déjà documenté (Expérience 024, `image` conditionnel non reconnu par l'heuristique regex) sur `slide`/`news`, sans rapport avec ce lot.

**Tâche #51 marquée complétée. Aucune tâche de suivi non demandée restante sur ce sujet précis** — la seule limite explicitement documentée (forme du VO de filtre non alignée sur `DatePeriod` bout en bout jusqu'au mapper API) est un choix de scope assumé, pas un oubli, et ne concerne que ces 3 entités.

## Expérience 028 — Revue critique du module de référence après modification utilisateur, puis correction (administrative-infrastructure)

**Directive reçue** : l'utilisateur a modifié lui-même `administrative-infrastructure` ("séparation des responsabilités et des types") et a demandé une critique honnête, niveau ingénieur-chercheur Microsoft/Google Research, sur l'état du module. Revue effectuée par lecture directe du code réel (pas de confiance en mémoire), `git status`/timestamps pour isoler les fichiers effectivement touchés (159 fichiers dans ce module seul), puis `tsc`/`check-semantics.js` en vérification croisée.

**Constats positifs, vérifiés** : renommage `presentation/{entity}` → `presentation/features/{entity}` (aligne enfin ce module sur la convention de dossier déjà utilisée partout ailleurs — content-management, settings-security, team-organization) ; déplacement de `domain/controls/` vers `presentation/store/` (correction de couche réelle : un type `FormControl` est une préoccupation UI, pas domaine) ; suppression complète de `responsibilities-select` dans ce module (confirmé résidu de copier-coller sans rapport, la fonctionnalité homonyme légitime de `settings-security/users` n'a pas été touchée) ; `tsc` à 0 erreur sur l'app entière malgré l'ampleur du changement.

**Bug systémique trouvé, confirmé par lecture puis par grep** : l'ajout de Contract/ValidateContract/validateur sur `delete`/`filter`/`find-one-filter` (et `enable`/`disable` pour `infrastructure-type`) a introduit des validateurs qui lèvent maintenant une exception **synchrone** (`GenericRequiredError`, `DateRangeInvalidError`), exactement le motif documenté depuis Expérience 008. Mais aucune des méthodes de use-case correspondantes n'était enveloppée dans `defer()` — seuls `create()`/`update()` l'étaient. Un throw synchrone hors `defer()` ne devient jamais une erreur RxJS propre pour l'abonné, il sort comme une exception JS au moment de l'appel. Confirmé sur **8 méthodes** au total : `InfrastructureUseCase.execute()`/`.delete()`, `InfrastructureTypeUseCase.execute()`/`.delete()`/`.enable()`/`.disable()`, `InfrastructureFindOneUseCase.execute()`, `InfrastructureTypeFindOneUseCase.execute()`.

**Angle mort de l'outillage constaté** : `check-semantics.js` (règle 1) répondait "OK" sur les deux entités malgré le bug — la règle ne teste littéralement que `create()`/`update()`, jamais `delete()`/`enable()`/`disable()`/`execute()`. Le bug a donc traversé la seule vérification automatisée du projet sans être détecté ; seule la lecture manuelle du code l'a révélé. Limite documentée, non corrigée dans cette tâche (extension de la règle 1 non demandée explicitement, laissée en tâche de suivi potentielle).

**Incohérence de nommage trouvée et arbitrée par les données, pas par préférence** : `infrastructure-type` gardait la convention tiret (`infrastructure-type-repository.impl.ts`) tandis que `infrastructure` venait de passer à une convention point (`infrastructure.repository.impl.ts`) pour ses 3 fichiers d'implémentation de repository — les deux entités du même module de référence n'étaient plus alignées entre elles. Vérification app-wide avant de trancher : sur les fichiers `*-repository.ts` abstraits (hors impl), le tiret domine largement (65 occurrences contre 33 en point) ; sur les fichiers `*.repository.impl.ts` spécifiquement, c'est un partage quasi égal (54 point contre 53 tiret) — aucune majorité claire à ce niveau précis, mais le signal le plus fort et le plus large (abstrait, 2:1) penche pour le tiret. Décision : harmonisation sur tiret, donc retour en arrière sur les 3 fichiers impl d'`infrastructure` (`infrastructure-repository.impl.ts`, `infrastructure-find-one-repository.impl.ts`, `infrastructure-select-repository.impl.ts`), pas l'inverse. 3 fichiers `di/infrastructure/*.providers.ts` mis à jour en conséquence.

**Nettoyage de code de debug** : `infrastructure-form.store.ts` portait un `effect()` entier dans le constructeur dont le seul contenu était `console.log('position signal', this.infrastructureType())` (le label ne correspondait même plus à la variable loggée) — bloc supprimé intégralement, pas seulement la ligne de log, puisqu'il ne servait plus à rien sans elle. `infrastructure-form.component.ts` portait un `console.log('result: ', result)` juste avant le `if (result)` qui gérait déjà le cas — ligne retirée.

**Vérifié** : grep app-wide confirmant zéro référence orpheline aux 3 anciens chemins `.repository.impl.ts` et zéro `console.log`/`console.debug` restant dans le module. `tsc -p tsconfig.app.json --noEmit` : 0 erreur sur l'app entière. `eslint --fix` sur les 12 fichiers touchés (3 repository impl renommés, 3 providers, 4 use-cases, 1 store, 1 component) : 0 erreur. `check-semantics.js` relancé sur les 2 entités : toujours "OK" sur la règle 1 — confirmation, pas contradiction, de l'angle mort déjà documenté (la règle ne peut physiquement pas détecter ce que corrige cette tâche).

**Tâche #52 marquée complétée.** Limite ouverte, non traitée ici, laissée pour suite éventuelle : étendre `check-semantics.js` règle 1 pour couvrir `delete()`/`enable()`/`disable()`/`execute()` en plus de `create()`/`update()`, afin que ce bug de classe (déjà vu 3 fois : Expérience 008/012, Expérience 027 sur home/slide/news, et ici sur le module de référence lui-même) devienne mécaniquement détectable plutôt que dépendant d'une relecture manuelle à chaque fois.

**Correction (voir Expérience 029) : la décision de nommage ci-dessus (paragraphe "Incohérence de nommage...") est invalidée et remplacée.** Le raisonnement par comptage de fréquence (65 vs 33, 54 vs 53) était une mauvaise méthode pour un module destiné à devenir la référence — cf. Expérience 029.

## Expérience 029 — Correction méthodologique du nommage (convention documentée, pas comptage) + règle 1 rendue précise + nouveau bug défer() trouvé (administrative-boundary)

**Directive reçue, critique explicite de l'architecte** : après lecture de la décision de nommage de l'Expérience 028 (tiret choisi par comptage de fréquence app-wide), l'architecte a rejeté la méthode elle-même, pas seulement le résultat : *"nous sommes au stade de construction d'un module qui servira de référence, par conséquent le fait de déduire que tel ou tel cas domine [dans le code existant] devrait devenir la convention du module de référence, est une mauvaise logique."* Règle méthodologique à retenir pour la suite du projet, verbatim : *"la convention vient des documentations, ou des recherches vérifiées et approuvées"* — jamais du comptage d'usage existant dans une base de code dont les conventions sont elles-mêmes reconnues incohérentes et en cours de correction. Demande explicite associée : étendre la règle 1 de `check-semantics.js` à toute méthode de use-case, pas seulement `create()`/`update()`.

**Correction du nommage repository, par source documentée** : recherche de la convention officielle Angular (style guide, v17/v18/v19 — `v19.angular.dev/style-guide`, `v17.angular.io/guide/styleguide`, `v18.angular.dev/style-guide/`) : règle documentée = tirets à l'intérieur du nom descriptif, point avant le suffixe de type (`feature.type.ts`, ex. `hero-list.component.ts`). Vérification croisée contre le module réel : 135/142 fichiers de `administrative-infrastructure` (mapper, dto, vo, validator, contract, validate-contract, providers, entity, use-case, facade, component, api, store, control) suivaient déjà cette convention point — seul `repository` faisait exception. Décision (annule et remplace celle de l'Expérience 028) : convention point pour `repository`, alignée sur le reste du module. 12 fichiers renommés (6 abstraits dans `domain/repositories/`, 6 impl dans `infrastructure/data/repositories/`, couvrant `infrastructure` ET `infrastructure-type` — ce dernier jamais migré avant), 12 fichiers consommateurs mis à jour (6 use-cases, 6 providers). Vérifié : grep zéro référence orpheline aux anciens chemins, `tsc -p tsconfig.app.json --noEmit` 0 erreur, `eslint --fix` sur les 24 fichiers touchés 0 erreur.

**Bug défer() supplémentaire trouvé pendant ce nettoyage** : `InfrastructureSelectUseCase.readAll()` et `InfrastructureTypeSelectUseCase.readAll()` appelaient `this.repository.readAll(options)` sans `defer()` — corrigé de la même façon (ces deux méthodes ne valident rien, donc ne peuvent pas throw ; corrigé par cohérence/prudence, pas par nécessité stricte détectée par l'outil).

**Règle 1 de `check-semantics.js` étendue puis rendue précise** : première extension — passage d'une liste figée `['create', 'update']` sur un seul fichier `{entity}.use-case.ts` à une détection générique par signature (`extractUseCaseMethodNames`, regex sur `): Observable<`) balayant tous les fichiers `*.use-case.ts` du dossier `application/use-cases/{entity}/`. Cette première version était trop large : testée sur `content-management/home`, elle signalait à tort `enable()`/`disable()`/`delete()` de `home.use-case.ts` et `execute()` de `home-find-one.use-case.ts` comme nécessitant `defer()`, alors que leurs VO sont de purs passthrough (`(dto) => dto`) qui ne peuvent jamais throw — faux positifs confirmés, exactement le type d'erreur de raisonnement que l'architecte vient de signaler.

**Correction de précision** : ajout de `extractImportMap`, `extractCalledIdentifiers`, et `fileCanThrowSynchronously` (résolution récursive des alias tsconfig, profondeur 3, détection de `throw new` dans le fichier appelé ou toute fonction qu'il appelle à son tour). La règle ne signale désormais un défer manquant que si un identifiant réellement appelé dans le corps de la méthode (ex. le VO passé au repository) résout vers un fichier — ou une fonction qu'il appelle — capable de lever une exception synchrone.

**Vérification, par exécution réelle, pas par confiance dans la logique du code** :
- `administrative-infrastructure` / `infrastructure`, `infrastructure-type` : "OK", aucun faux positif restant.
- `content-management` / `home`, `slide`, `news` : "OK" — les faux positifs sur `enable`/`disable`/`delete`/`find-one` ont disparu.
- `processing` / `tasks`, `settings-security` / `users`, `settings-security` / `profiles-permissions`, `team-organization` / `participants`, `team-organization` / `teams` : "OK" sur les 5 (le check complet sur `profiles-permissions` s'est révélé trop lent en pratique à cause de la règle 5, préexistante et non liée à ce travail — cf. limite ci-dessous ; règle 1 isolée testée directement : "OK", ~2,7s).
- `administrative-boundary` / `departments`, `regions`, `municipalities` : règle 1 a signalé un **vrai bug** — `create()`/`update()` des 3 entités appellent `{entity}CreateVo`/`{entity}UpdateVo` sans `defer()`, et ces VO appellent bien un validateur qui lève `GenericRequiredError` de façon synchrone (confirmé par lecture directe du validateur et du VO). Même classe de bug qu'Expérience 008/012/027/028, jamais détectée jusqu'ici sur ce module — probablement simplement jamais passé par cet outil, pas un cas que l'ancienne règle aurait ratée (elle testait déjà `create`/`update` par nom exact, ce qui aurait matché ici). **Non corrigé dans cette tâche** : `administrative-boundary` n'est pas le module en cours de revue (`administrative-infrastructure`), correction laissée en attente d'arbitrage explicite plutôt que faite par extension de périmètre non demandée.

**Limite découverte, non corrigée ici** : la règle 5 (artefacts orphelins) relit chaque fichier `.ts` de toute l'application (~3900 fichiers) une fois par classe candidate trouvée dans `commands/dto/entities` de l'entité — sur des entités à beaucoup de sous-cas (ex. `profiles-permissions` : create/update/enable/disable/delete/filter/find-one/select/users/free-users/permissions/assign/remove/reassign), ce coût devient prohibitif (timeout observé). Préexistant, indépendant de cette tâche, non corrigé.

**Tâche #53 marquée complétée** pour sa partie demandée (nommage tranché par source documentée, règle 1 étendue et rendue précise, vérifiée sur 13 entités). **Point en attente de décision de l'architecte** : les 3 bugs défer() réels trouvés dans `administrative-boundary` (departments/regions/municipalities, create+update) — corriger maintenant ou traiter séparément ?

**Directive reçue** : recentrer l'effort exclusivement sur `administrative-infrastructure` (le module de référence), reporter `administrative-boundary` et les autres à plus tard, une fois le module de référence consolidé. Nouvelle critique demandée sur l'état actuel du module.

**Critique fraîche effectuée (relecture directe, pas de mémoire)** : `tsc --noEmit` 0 erreur ; `check-semantics.js` (6 règles) "OK" sur `infrastructure` ET `infrastructure-type` ; zéro `console.log` résiduel ; les deux entités symétriques sous `presentation/features/`. Deux défauts réels trouvés :

1. **Schéma canonique en retard sur le module qu'il valide** : `check-pattern.js` signalait 3 fichiers "manquants" (`{entity}[-find-one|-select]-repository.ts`) sur `infrastructure` et `infrastructure-type`, alors qu'ils existent — sous leur nouveau nom point (Expérience 029). `crud-entity.pattern.json` avait été extrait automatiquement d'`administrative-boundary`, qui garde encore l'ancien nom tiret pour ces mêmes fichiers. Corrigé : les 3 entrées `domain/repositories/{ENTITY}/...` du schéma passées en convention point (lignes 53-55), avec une note `known_deviation_v10` documentant explicitement que `administrative-boundary` va maintenant apparaître non conforme sur ces 3 fichiers jusqu'à sa propre migration (conséquence attendue, pas un bug de l'outil, et pas une raison de revenir en arrière sur le schéma).
2. **Asymétrie de nommage entre les deux entités du module** : `infrastructure` a `infrastructure-select-response-api.dto.ts` ; `infrastructure-type` avait le même contenu exact (`{Entity}SelectItemApiDto` + `{Entity}SelectResponseApiDto`) mais dans un fichier nommé `infrastructure-type-select-api.dto.ts` (sans "-response"). Renommé en `infrastructure-type-select-response-api.dto.ts`, 3 fichiers consommateurs mis à jour (`infrastructure-type-select.entity.ts`, `infrastructure-type-select.mapper.ts`, `infrastructure-type-select.api.ts`).

**Vérifié après correction** : `check-pattern.js` sur `infrastructure` et `infrastructure-type` → 83/83 (100.0%), zéro fichier manquant. Grep zéro référence orpheline à l'ancien nom de fichier DTO. `tsc -p tsconfig.app.json --noEmit` 0 erreur. `eslint --fix` sur les 4 fichiers touchés (DTO renommé + 3 consommateurs) : 0 erreur. JSON du schéma revérifié valide (`JSON.parse`).

## Expérience 030 — Règle 7 de check-semantics.js : nommage {ENTITY}-select-response-api.dto.ts

**Directive reçue** : formaliser en pattern mécanique le bug de nommage trouvé et corrigé sur `infrastructure-type` (Expérience 029, DTO select mal nommé), et mettre à jour la documentation (schéma + register) en conséquence.

**Règle ajoutée à `check-semantics.js`** (7e vérification, docstring et rapport mis à jour en conséquence) : quand une entité a un repository select en convention point (`domain/repositories/{ENTITY}/{ENTITY}-select.repository.ts` — le déclencheur, signe que la fonctionnalité select existe réellement pour cette entité), le DTO d'API correspondant doit exister exactement sous `infrastructure/api/dto/{ENTITY}/{ENTITY}-select-response-api.dto.ts`. Si absent, la règle cherche activement un fichier `*select*.ts` voisin dans le même dossier avant de conclure : si un candidat existe, elle le signale explicitement comme "probable variante de nommage" plutôt que de rapporter une simple absence — message actionnable, pas un diagnostic générique.

**Vérifié par exécution réelle** : `infrastructure` et `infrastructure-type` → règle 7 "OK" (le fichier existe désormais sous le bon nom pour les deux). Déclencheur testé par `find` app-wide : seuls `infrastructure` et `infrastructure-type` ont un repository select en convention point à ce jour — la règle ne peut donc pas encore se déclencher ailleurs, zéro risque de faux positif introduit par cet ajout.

**Constat fait en vérifiant l'ampleur réelle du problème (par `find`, pas par supposition)** : la convention canonique `{ENTITY}-select-response-api.dto.ts` (déjà dans `crud-entity.pattern.json` core_files depuis la v1) est en réalité déjà suivie par `administrative-boundary` (departments/regions/municipalities), `content-management` (news-categories) et `seos-reference` (resources). En revanche `settings-security` (profiles-permissions, users, responsibilities) et `team-organization` (participants, roles, teams) suivent une DEUXIÈME convention, différente mais interne à ces deux modules et cohérente en leur sein : `{ENTITY}-select-api.dto.ts` (sans "-response"), vérifié par lecture de 2 fichiers (contenu identique à leur équivalent canonique, seul le nom de fichier diffère — même défaut structurel qu'`infrastructure-type`, mais pas un accident isolé cette fois, un choix systématique sur 6 fichiers). **Non corrigé** : directive explicite de l'architecte de rester concentré sur `administrative-infrastructure` pour cette session ; `settings-security`/`team-organization` gardent de toute façon leur repository select en convention tiret (jamais migrés), donc la règle 7 ne se déclenche pas dessus pour l'instant — elle s'activera d'elle-même le jour de leur migration.

**Documentation mise à jour** : `crud-entity.pattern.json` version 11 → 12, nouvelle entrée `design_decisions_v12` (règle + état réel constaté par `find`, avec le même luxe de précision que les décisions précédentes — pas de comptage présenté comme un verdict, juste un état des lieux factuel en attente de décision).

**Vérifié** : `node -c` sur `check-semantics.js` (syntaxe), exécution réelle sur `infrastructure`/`infrastructure-type` (règle 7 "OK", 7 règles au total désormais), JSON du schéma revalidé (`JSON.parse`).

## Expérience 031 — Règle 8 de check-semantics.js : chaînage VO → Entity, généralisé à toute méthode de use-case, toute l'application

**Directive reçue** : l'architecte a signalé un modèle déjà existant mais jamais rendu mécanique — après validation par le VO (create/update/enable/etc.), l'Entity peut suivre le VO quand il y a une règle métier réelle à implémenter, exactement comme déjà fait pour le filtre (`infrastructureFilterVo()` puis `infrastructureFilterEntity()`, qui applique le défaut `endDate = aujourd'hui` si `startDate` est présent sans `endDate`). Demande explicite : prendre ceci en compte pour tous les use-cases de l'application, pas seulement `infrastructure`.

**Constat** : `design_decisions_v7` (schéma) avait déjà posé la règle générale (VO toujours fonction ; Entity uniquement si transformation réelle, pour create/update/delete/filter/enable/disable) mais seul le sens "Entity fantôme à supprimer" était vérifié mécaniquement (règle 6). Le sens inverse — une Entity qui contient une vraie règle mais n'est plus appelée depuis le use-case (régression silencieuse, aucune erreur tsc/eslint ne peut la révéler puisque le fichier compile, il est juste devenu orphelin d'appel) — n'était couvert par rien.

**Règle 8 ajoutée à `check-semantics.js`** (docstring et rapport mis à jour, 7 → 8 règles) : pour chaque méthode de use-case, si elle appelle un VO classless (`{entity}-{op}.vo`), et que `domain/entities/{entity}/{entity}-{op}.entity.ts` existe avec une logique conditionnelle réelle détectée (même heuristique que la règle 6), alors une fonction résolvant vers ce fichier Entity doit aussi être appelée dans le même corps de méthode — sinon signalement explicite.

**Vérifiée par un scénario contrôlé, pas seulement par lecture** : fichiers de test temporaires créés dans `administrative-infrastructure` (entité fictive `widget` : VO filtre classless + Entity avec vraie règle conditionnelle + use-case). Version cassée (VO appelé, Entity jamais appelée) → règle 8 signale correctement le problème avec un message actionnable. Version corrigée (Entity chaînée après le VO) → règle 8 repasse "OK". Fichiers de test supprimés immédiatement après, `git status` confirmé propre (aucune trace).

**Testée en lecture seule sur 13 entités réelles** (aucune correction appliquée, conformément à la directive de rester concentré sur `administrative-infrastructure`) : `infrastructure`, `infrastructure-type`, `resources` (seos-reference), `departments`/`regions`/`municipalities` (administrative-boundary), `home`/`slide`/`news` (content-management), `tasks` (processing), `users`/`profiles-permissions` (settings-security), `participants`/`teams` (team-organization) — "OK" partout.

**Limite honnête découverte pendant cette vérification, documentée dans le schéma (`design_decisions_v13.limite_de_detection_connue`)** : la règle 8 ne reconnaît que le style d'appel classless (fonction nue, ex. `infrastructureFilterVo(x)`). Les ~13 modules non encore migrés à cette convention (dont `administrative-boundary`, vérifié directement sur `departments-filter.entity.ts`) utilisent encore l'ancien style à base de classes (`DepartmentsFilterVo.fromDto(dto)` puis `DepartmentsFilterEntity.fromVo(vo)`) — la règle ne détecte pas du tout ces appels, donc leur "OK" signifie "non testé", pas "vérifié sain". Vérifié sur `departments-filter.entity.ts` : même dans ce cas non détecté, le fichier ne contient aucune logique conditionnelle réelle (recopie de champs uniquement), donc rien n'aurait été signalé de toute façon — mais ce n'est pas une garantie généralisable aux ~12 autres modules non-classless, seulement une observation sur ce cas précis.

**Vérifié** : `node -c` sur `check-semantics.js` (syntaxe), scénario synthétique (faux positif/négatif testés), 13 entités réelles testées en lecture seule, JSON du schéma revalidé (`JSON.parse`), aucun fichier de test résiduel dans le dépôt.

## Expérience 032 — Clôture de l'étape : promotion select-props au coeur, annotation datée sur la tranche select orpheline

**Directive reçue** : suite à la critique du 15/07 sur `administrative-infrastructure` (en particulier `infrastructure`), l'architecte a demandé de mettre à jour le schéma pour clore cette étape, avec deux consignes précises : (1) la tranche `InfrastructureSelectFacade` non consommée doit être annotée par anticipation avec une date de révision fixée au 30/07 ; (2) la mise à jour du schéma doit se référer explicitement au module `administrative-infrastructure` (le module de référence), pas à une majorité de corpus ailleurs.

**Promotion de `{ENTITY}-select-props.interface.ts` au coeur canonique** : ajouté à `core_files` de `crud-entity.pattern.json` (v13 → v14). Décision explicitement ancrée sur `administrative-infrastructure` (seule occurrence dans toute l'app à ce jour, 2 fichiers, `infrastructure` et `infrastructure-type`) — cohérent avec la règle retenue en Expérience 029 : pour un module de référence, la convention vient d'une décision d'architecte ancrée sur ce module, pas d'un comptage majoritaire ailleurs. Cette extraction reproduit exactement la forme déjà promue pour `{ENTITY}-find-one-props.interface.ts` (Expérience 009/v9), rendant les trois entities (principale, find-one, select) symétriques.

**Tranche `infrastructure` select annotée par anticipation, avec date de révision** : `InfrastructureSelectFacade` (et toute la chaîne associée) reste sans consommateur réel (reconfirmé) mais n'est plus laissée en suspens indéfiniment — documentée dans `design_decisions_v14.infrastructure_select_slice_anticipee` avec la même règle que `commands_mappers_contracts_factories` (annotation explicite + date de révision, pas un argument infalsifiable). Honnêteté maintenue : aucun scénario concret de consommation n'est inventé pour justifier sa conservation — le champ le dit explicitement ("NON IDENTIFIE à ce jour"). Date de révision : **2026-07-30**. Si aucun consommateur réel n'existe ou n'est planifié à cette date, la tranche devra être câblée à un besoin réel ou supprimée.

**Vérifié** : `check-pattern.js` sur `infrastructure` et `infrastructure-type` → 84/84 (100.0%) après l'ajout au schéma (contre 83/83 avant), confirmant que le fichier existait déjà des deux côtés et que l'ajout au schéma ne casse rien. JSON du schéma revalidé (`JSON.parse`).

## Expérience 033 — Durcissement de l'analyse : règle 9 (résidus console.*) + extension de la règle 5 aux facades

**Directive reçue** : « augmente les étapes d'analyse en te fiant au module de référence, pour une analyse plus stricte. » Deux ajouts, tous deux directement dérivés de bugs réels déjà trouvés sur `administrative-infrastructure` cette session, pas inventés.

**Règle 9 ajoutée** (docstring et rapport mis à jour, 8 → 9 règles) : tout fichier dont un segment de chemin égale exactement le nom de l'entité (évite toute collision `infrastructure` / `infrastructure-type`, vérifiée par comparaison de segment, pas par sous-chaîne) ne doit contenir aucun `console.log`/`console.debug`/`console.warn`. `console.error` volontairement exclu (peut être une stratégie de logging légitime). Modèle : les deux `console.log` trouvés et supprimés manuellement en Expérience 028 (`infrastructure-form.store.ts`, `infrastructure-form.component.ts`), jamais détectés par `tsc`/`eslint`.

**Règle 5 étendue** : `application/services/{ENTITY}` ajouté aux dossiers scannés pour artefacts orphelins (en plus de commands/dto/entities), pour couvrir les facades — directement motivé par `InfrastructureSelectFacade`, trouvée sans consommateur uniquement par grep manuel (critique du 15/07, Expérience 032), jamais signalée mécaniquement jusqu'ici.

**Vérifié par exécution réelle complète (les 9 règles, pas seulement les 2 nouvelles)**, sur les deux entités du module de référence :
- `infrastructure` (38s... en réalité ~1,5s CPU, le reste en I/O du sandbox) : règle 5 signale correctement `InfrastructureSelectFacade` comme orpheline — attendu, déjà expliqué par `design_decisions_v14`, pas un nouveau bug. Règle 9 trouve un `console.warn` **réel, jusque-là inconnu** dans `infrastructure-list.component.ts` (`onTableAction`) : deux gardes défensives (`'Missing actionId'`, `'Unknown action:'`) sur un évènement de table malformé.
- `infrastructure-type` : règle 5 "OK" (confirme que `InfrastructureTypeSelectFacade`, elle, a de vrais consommateurs — cohérent avec Expérience 032). Règle 9 trouve le **même motif exact**, symétrique, dans `infrastructure-type-list.component.ts`.

**Lecture honnête de cette dernière trouvaille, pas de verdict automatique** : la règle 9 est volontairement une heuristique regex, pas un jugement sémantique. Les deux `console.warn` trouvés ne ressemblent pas au profil de code mort d'Expérience 028 (un `console.log` sans aucune autre utilité) — ce sont des garde-fous défensifs sur un évènement de table qui ne devrait jamais arriver, présents de façon symétrique et délibérée sur les deux entités. Candidat légitime à discuter (faut-il les faire remonter via `UiFeedbackService` comme le reste du pipeline d'erreur, ou `console.warn` est-il un choix assumé pour une erreur de programmation plutôt qu'une erreur métier ?) mais PAS un cas évident de résidu à supprimer — signalé tel quel, décision laissée à l'architecte.

**Vérifié** : `node -c` sur `check-semantics.js` (syntaxe), exécution complète (9/9 règles) sur `infrastructure` et `infrastructure-type`, aucune régression sur les 7 autres règles.

## Expérience 034 — Suppression des console.warn + robustesse TOCTOU de l'outil + clôture

**Suppression confirmée** : l'architecte a retiré les deux `console.warn` trouvés en Expérience 033. Vérifié par grep frais (`console\.(log|debug|warn)` sur tout `administrative-infrastructure`) : zéro résultat. Règle 9 ré-exécutée sur `infrastructure` et `infrastructure-type` : "OK" sur les deux.

**Bug d'outillage trouvé et corrigé en cours de vérification** : `check-semantics.js` a planté deux fois (ENOENT) en relisant sans garde `fs.readFileSync` des fichiers listés par le scan initial (`allTsFiles`) mais renommés/supprimés entre-temps par une édition concurrente ailleurs dans le dépôt (`tests-reference`/`seos-reference`, hors périmètre de cette tâche). Corrigé : les deux relectures aveugles (règle 3, comparaison DomainError ; règle 5, comptage de références) passent désormais par `readIfExists` et ignorent silencieusement un fichier disparu, au lieu de planter tout le script. Vérifié : `node -c` (syntaxe), ré-exécution complète sans crash sur `infrastructure`/`infrastructure-type`.

**État final vérifié, les 9 règles + structure** : `infrastructure` — 9/9 "OK" (seul signalement : `InfrastructureSelectFacade` orpheline, règle 5, déjà expliqué et daté au 30/07 en Expérience 032, pas un nouveau problème). `infrastructure-type` — 9/9 "OK" sans aucun signalement. `check-pattern.js` : 84/84 (100%) sur les deux. `tsc --noEmit` : 0 erreur sur le périmètre `administrative-infrastructure`.

**Étape déclarée close** : le module de référence `administrative-infrastructure` (entités `infrastructure` et `infrastructure-type`) est maintenant propre sur toute la ligne mécanique disponible (9 règles de contenu + conformité structurelle 100% + compilation + lint), avec un seul point ouvert explicitement daté (30/07). Reste en attente, explicitement reporté par l'architecte depuis plusieurs expériences, pour une passe ultérieure : les 3 bugs défer() réels sur `administrative-boundary` (departments/regions/municipalities, Expérience 029) et la convergence de nommage `{ENTITY}-select-api.dto.ts` → `{ENTITY}-select-response-api.dto.ts` sur `settings-security`/`team-organization` (6 fichiers, Expérience 030).

## Expérience 035 — Réécriture complète de `generate-reference-module.js` sur `administrative-infrastructure` tel qu'il est aujourd'hui

**Déclencheur** : l'architecte a signalé que `seos/tools/generate-reference-module.js` ne respectait plus strictement la conception du module de référence `administrative-infrastructure`, et a demandé une réécriture intégrale ("réécris le générateur entièrement pour qu'il colle au module de référence tel qu'il est aujourd'hui").

**Méthode** : aucune supposition ni recopie de l'ancien générateur. Chaque couche (contrats/validate-contracts/validateurs/VO, entités, repositories abstraits et impl, mappers, DTO d'API, sources API, providers DI, commands/queries/handlers/bus/mappers applicatifs, facade, présentation) a été relue directement dans le code réel de `infrastructure` avant d'écrire la ligne équivalente du générateur, y compris pour des hypothèses qui semblaient acquises (ex : "delete n'a pas besoin de Contract" — vérifié faux par lecture de `infrastructure-delete.{contract,validate-contract,validator}.ts`).

**Écarts réels trouvés et corrigés dans le générateur** (tous vérifiés par lecture directe, pas par mémoire) :
1. Repositories abstraits en convention tiret au lieu de la convention point (Expérience 029).
2. `defer()` absent sur `execute()` (filtre) et `delete()` alors que leurs VO/validateurs peuvent lever une exception synchrone — même classe de bug que les Expériences 008/012/027/028/029, jamais corrigée dans le générateur lui-même jusqu'ici.
3. `delete` et `find-one-filter` traités comme des VO identité purs sans Contract/ValidateContract/Validator — faux, les deux ont un vrai chaînage avec `GenericRequiredError` sur `uniqId` manquant dans le code réel.
4. Entité/mapper/DTO de select non alignés sur la forme réelle (interface Props dédiée, mapper `@Injectable` étendant `ArrayResponseMapper`, DTO de réponse enveloppé dans `SimpleResponseDto<T[]>` au lieu d'un tableau nu).
5. Retour des mutations en `SimpleResponseDto<void>` au lieu de `MessageResponseDto`.
6. Providers DI nommés `provide{Cap}` au lieu de `{entity}Providers: Provider[]`.
7. Source API avec URL en dur au lieu de `SETTINGS_API_URL` injecté + fichier d'endpoints dédié + `buildHttpParams`/`buildHttpPayload`.
8. Facade principale minimale au lieu du modèle réel (signaux d'état d'action, suivi mémoire, `handleObservableWithFeedback`).

**Décision de périmètre explicite** : la couche présentation du générateur reste volontairement générique (pas de sélecteur de type croisé, pas de géolocalisation, pas de tooltips de permission) — ces enrichissements sont propres au besoin métier d'`infrastructure`, pas à l'architecture CRUD elle-même, qui est ce que ce générateur doit reproduire fidèlement.

**Gap de schéma trouvé au passage** : `crud-entity.pattern.json` (`core_files`) omettait entièrement `domain/contracts/{ENTITY}/{ENTITY}-{create,update,delete,find-one-filter}.contract.ts`, leurs `.validate-contract.ts`, et `domain/validators/{ENTITY}/{ENTITY}-{create,update,delete,find-one-filter}.validator.ts` — 12 fichiers pourtant réels et actifs dans `administrative-infrastructure`. Schéma corrigé en v15 (84 → 96 fichiers), décision ancrée explicitement sur `administrative-infrastructure` (pas sur un comptage corpus), conformément à la méthodologie retenue en Expérience 029/032. Voir `design_decisions_v15` dans le schéma pour le détail complet.

**Vérification** : générateur syntaxiquement valide (`node -c`), génère 100 fichiers sans erreur. Testé en le générant temporairement dans `src/presentation/pages/seos-reference` (jamais commit, supprimé immédiatement après chaque vérification, confirmé vide par `git status --short`) : `tsc -p tsconfig.app.json --noEmit` sur l'application entière avec le module généré présent → 0 erreur. `eslint --fix` puis `eslint` → 0 erreur restante (46 erreurs initiales, toutes des nits de formatage prettier, aucune règle de logique). `check-pattern.js` → 96/96 (100%) sur le schéma v15 fraîchement mis à jour. `check-semantics.js` (9 règles) → OK sur 7 des 9 règles ; règle 2 (clés i18n) signale 3 clés absentes de `fr.json` et règle 5 (artefacts orphelins) signale `ResourcesFindOneFacade`/`ResourcesSelectFacade` non consommées — les deux attendus et documentés (module de référence synthétique non branché aux routes/traductions réelles, couche présentation volontairement minimale), pas des défauts du générateur.

**État** : le générateur est maintenant en cohérence avec le schéma v15 et le module de référence réel. Reste en attente, explicitement reporté par l'architecte : les 3 bugs défer() sur `administrative-boundary`, la convergence de nommage select-api.dto sur `settings-security`/`team-organization`, et le point de révision daté du 30/07 sur `InfrastructureSelectFacade` (Expérience 032). Décision non prise et non demandée à ce jour : régénérer réellement le module `seos-reference` dans le dépôt avec ce nouveau générateur (actuellement supprimé après chaque test, jamais laissé en place).

## Expérience 036 — Correction des 3 bugs défer() sur `administrative-boundary` (departments/regions/municipalities)

**Contexte** : parmi les chantiers explicitement reportés par l'architecte depuis l'Expérience 029, le plus prioritaire d'un point de vue ingénierie (pas cosmétique/conventionnel comme les deux autres) est le bug défer() réel : `departments.use-case.ts`, `regions.use-case.ts` et `municipalities.use-case.ts` appelaient `{entity}CreateVo`/`{entity}UpdateVo`/`{entity}DeleteVo` (des assertion functions qui lèvent `GenericRequiredError` de façon synchrone si un champ requis manque) directement dans le corps de `create()`/`update()`/`delete()`, sans les envelopper dans `defer()`. Conséquence concrète : une erreur de validation devient une exception JavaScript synchrone non interceptée, au lieu d'une erreur RxJS que `catchError`/`UiFeedbackService` peuvent traiter — l'utilisateur ne voit jamais le message d'erreur, l'application peut planter au niveau composant.

**Vérification fraîche avant correction** : lecture directe des 3 fichiers (pas de mémoire) — confirmé identique sur les 3 entités. `execute()` (filtre) et `delete()` utilisent encore le style classe historique (`{Entity}FilterVo.fromDto()`/`{Entity}FilterEntity.fromVo()`, `{Entity}DeleteVo.fromDto()`/`{Entity}DeleteEntity.fromVo()`) — non migré au style classless (Expérience 010), hors périmètre de cette correction, qui porte uniquement sur `defer()`.

**Correction** : les 4 méthodes (`execute`, `create`, `update`, `delete`) des 3 fichiers sont désormais enveloppées dans `defer(() => ...)`, sur le modèle déjà appliqué à `administrative-infrastructure` (Expérience 008) et au générateur de référence réécrit (Expérience 035). Portée strictement limitée à ce bug — aucune migration classless entreprise ici (chantier distinct, explicitement séparé par l'architecte).

**Vérification** : `tsc -p tsconfig.app.json --noEmit` sur l'application entière → 0 erreur sur `administrative-boundary`. `eslint --fix` puis `eslint` sur les 3 fichiers → 0 erreur restante (2 nits de formatage prettier initiaux, corrigés).

**État** : les 3 bugs défer() identifiés en Expérience 029 sont corrigés. Restent en attente : la convergence de nommage select-api.dto (`settings-security`/`team-organization`), le point de révision du 30/07 sur `InfrastructureSelectFacade`, et la migration classless étendue (Expérience 010/013, ~13 modules).

## Expérience 037 — Premier outil d'extraction automatisée (`extract-pattern.js`), Étape 3 de `besoin-reformule-SEOS.md`

**Contexte** : relecture du document fondateur `besoin-reformule-SEOS.md` avec l'architecte. Son séquencement (section 4) prévoit : (1) concevoir l'IR, (2) construire un seul frontend (Angular/TS), (3) valider le pipeline complet extraction → Pattern Repository → génération → validation, (4) ajouter un deuxième frontend délibérément différent (Rust/Solana) pour stress-tester l'IR, (5) généraliser. Constat honnête fait avec l'architecte : les étapes 1/2/3 existaient déjà en partie (schéma v15, `generate-reference-module.js`, `check-pattern.js`, `check-semantics.js`), MAIS l'« extraction » n'a jamais été automatisée — le schéma est entièrement maintenu à la main, par lecture humaine (assistée IA) et décision d'architecte à chaque itération (v1 à v15), jamais par un mineur automatique comme le prévoyait la section 4.2 du document (« ts-morph pour parser l'AST »). L'architecte a choisi explicitement de combler cet écart avant d'envisager l'étape 4 (pas de codebase Rust/Solana réelle disponible dans ce dépôt de toute façon).

**Choix technique** : `ts-morph` n'est pas installé dans le projet ; plutôt que d'ajouter une dépendance à un projet de production pour un script de recherche, le script utilise directement l'API du compilateur TypeScript (`typescript`, déjà présente en dépendance, v6.0.3) — même capacité d'analyse AST réelle, zéro nouvelle dépendance.

**Ce que fait `seos/tools/extract-pattern.js`** : (1) découvre automatiquement les entités CRUD réelles du projet par signal structurel (un repository abstrait exposant `execute`/`readAll` + une entité principale, dans n'importe quel module sous `src/presentation/pages`) — 39 entités trouvées, pas une liste choisie à l'avance ; (2) sépare les entités à mutation réelle (`{entity}-create.vo.ts` présent, 17/39) des entités en lecture seule (tableaux de bord, journaux — 22/39), une distinction que le script découvre lui-même et qui corrige un dénominateur trompeur (sans cette séparation, les chemins create/update/delete semblaient présents dans seulement ~44% du corpus alors qu'ils sont en réalité à 100% du sous-corpus pertinent) ; (3) calcule la fréquence de chaque chemin normalisé ({ENTITY} substitué) à travers le corpus, sur les deux dénominateurs ; (4) extrait une empreinte structurelle légère (décorateurs, classe/fonction exportée, `extends`/`implements`, méthodes) via l'AST réel pour une famille de fichiers donnée (`--fingerprint repository|mapper|facade`) ; (5) compare au schéma `crud-entity.pattern.json` maintenu à la main.

**Résultat de la première exécution réelle (39 entités découvertes, 17 à mutation)** : sur le sous-corpus à mutation réelle (le dénominateur pertinent), le schéma v15 est en accord à 100% ou quasi (94%+) avec la fréquence observée pour la quasi-totalité des `core_files`. Zéro fichier trouvé à ≥80% de fréquence dans le corpus complet (39) qui serait absent du schéma — aucune découverte surprise à ce seuil. Sur le sous-corpus à mutation (17), 4 chemins apparaissent fréquents (82%, 14/17) mais absents du schéma : `{ENTITY}-find-one-filter.entity.ts`, `{ENTITY}-status.enum.ts`, et les 2 fichiers repository en convention tiret. Vérifié un par un contre le registre existant : les 4 sont des exclusions **déjà et explicitement documentées** par décision d'architecte (`design_decisions_v7`/`v8` pour l'entity-wrapper vide de transformation réelle, `design_decisions_v11` pour status.enum, `known_deviation_v10` pour la convention tiret non encore migrée) — pas des oublis. C'est une confirmation utile : les décisions passées de retrait du schéma n'étaient pas arbitraires, elles résistent à une mesure de fréquence indépendante et automatisée sur tout le corpus réel.

**Limites explicites du script** (à ne pas dépasser en lisant ses résultats, cohérent avec la discipline de toute la session) : (a) la découverte d'entités par signal structurel est une heuristique, pas une preuve — peut inclure des faux positifs ou manquer des variantes non standard ; (b) l'empreinte AST ne résout aucun type (pas de `TypeChecker`), elle lit la syntaxe uniquement ; (c) le script ne modifie JAMAIS `crud-entity.pattern.json` — il produit un rapport d'observation à soumettre à l'architecte, jamais un verdict à exécuter aveuglément, exactement la même règle que celle établie pour toutes les décisions de schéma depuis l'Expérience 004/029.

**État** : l'étape 3 du séquencement de `besoin-reformule-SEOS.md` a maintenant sa brique d'extraction automatisée manquante, testée sur un cas réel (tout le corpus du projet, pas un échantillon choisi), avec un résultat qui valide plutôt qu'il ne contredit le travail manuel accumulé depuis l'Expérience 001. L'étape 4 (deuxième frontend Rust/Solana) reste bloquée par l'absence de code réel de ce type dans ce dépôt — pas encore engagée, en attente d'une décision de l'architecte sur comment s'en procurer un si cette voie est poursuivie.

## Expérience 038 — Convergence de nommage `{ENTITY}-select-api.dto.ts` → `{ENTITY}-select-response-api.dto.ts` (settings-security, team-organization)

**Contexte** : dernier des 3 chantiers concrets restés en suspens depuis l'Expérience 029/030 (avec les bugs défer(), corrigés en Expérience 036). `--fingerprint mapper`/`--fingerprint facade` de `extract-pattern.js` n'ayant rien révélé de nouveau, ce chantier déjà entièrement spécifié était la suite la plus concrète disponible.

**Vérification fraîche avant correction** : 6 fichiers confirmés par `find` (pas par mémoire) : `profiles-permissions-select-api.dto.ts`, `responsibilities-select-api.dto.ts`, `users-select-api.dto.ts` (settings-security), `participants-select-api.dto.ts`, `roles-select-api.dto.ts`, `teams-select-api.dto.ts` (team-organization). Contenu de chacun vérifié identique en substance au modèle canonique (`SimpleResponseDto<{Cap}SelectItemApiDto[]>`) — seul le nom de fichier manquait le segment `-response`, comme documenté en `design_decisions_v12`. Chacun a exactement 3 importeurs (source API, mapper, entité), même schéma que le fix `infrastructure-type` de l'Expérience 029.

**Correction** : 6 fichiers renommés (`git mv`, historique préservé), 18 importeurs mis à jour (script Node dédié plutôt que `sed` en shell — une première tentative par `grep -rl | xargs sed` a silencieusement échoué à cause d'un problème de guillemets dans la commande, détecté en relisant un fichier avant de continuer plutôt qu'en supposant que ça avait marché).

**Vérification** : `tsc -p tsconfig.app.json --noEmit` → 0 erreur. `eslint` sur les fichiers touchés → 0 erreur. `grep` confirmant zéro référence résiduelle à l'ancien nom sur les 6 fichiers, 18 références au nouveau nom. Limite honnête : `check-semantics.js` règle 7 ne peut pas vérifier mécaniquement `responsibilities` et `roles` (entités select-only imbriquées dans les dossiers `users`/`participants`, sans leur propre segment de répertoire — hors de la portée de l'interface `(moduleRoot, entityName)` du script) ; vérifié directement à la place (tsc/eslint/grep, voir ci-dessus).

**État** : les 3 chantiers concrets identifiés en Expérience 029/030 sont maintenant tous traités (défer(), nommage repository sur administrative-infrastructure, nommage select-response-api.dto sur les 3 modules restants). Reste ouvert : le point de révision daté du 30/07 sur `InfrastructureSelectFacade` (pas encore échu), la migration classless étendue (Expérience 010/013, ~13 modules), l'étape 4 du besoin reformulé (deuxième frontend), et une observation faite en cours de route mais non traitée — une quantité significative de travail de cette session entière semble ne jamais avoir été committée dans git (`git status` révèle de nombreux fichiers modifiés/supprimés sans rapport avec ce fix précis, visiblement issus de tâches antérieures de la session) : à signaler à l'architecte, pas à committer unilatéralement.
