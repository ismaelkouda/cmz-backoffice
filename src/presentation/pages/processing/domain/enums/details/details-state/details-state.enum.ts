export enum DetailsState {
    PENDING = 'pending',
    APPROVED = 'approved',
    REJECTED = 'rejected',
    IN_PROGRESS = 'in-progress',
    COMPLETED = 'completed',
    TERMINATED = 'terminated',
}
