/* import { Provider } from '@angular/core';
import { MessagingRepositoryImpl } from '../data/repositories/messaging.repository.impl';
import { MessagingRepository } from '../domain/repositories/messaging.repository';

export const MESSAGING_PROVIDERS: Provider[] = [
    {
        provide: MessagingRepository,
        useClass: MessagingRepositoryImpl,
    },
];
 */