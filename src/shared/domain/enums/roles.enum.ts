export enum Roles {
    SUPERVISOR = 'COMMON.SUPERVISOR',
    LEADER = 'COMMON.LEADER',
    AGENT = 'COMMON.AGENT',
}

export enum RolesStyle {
    SUPERVISOR = 'info',
    LEADER = 'success',
    AGENT = 'dark',
}
